DROP TABLE mytab CASCADE CONSTRAINTS;

CREATE TABLE mytab
(
   v1      VARCHAR2 (10 BYTE)
 , v2      VARCHAR2 (10 BYTE)
 , v3      VARCHAR2 (10 BYTE)
 , v4      VARCHAR2 (10 BYTE)
 , v5      VARCHAR2 (10 BYTE)
 , v6      VARCHAR2 (10 BYTE)
 , v7      VARCHAR2 (10 BYTE)
 , v8      VARCHAR2 (10 BYTE)
 , v9      VARCHAR2 (10 BYTE)
 , v10     VARCHAR2 (10 BYTE)
 , v11     VARCHAR2 (10 BYTE)
 , v12     VARCHAR2 (10 BYTE)
 , v13     VARCHAR2 (10 BYTE)
 , v14     VARCHAR2 (10 BYTE)
 , v15     VARCHAR2 (10 BYTE)
 , v16     VARCHAR2 (10 BYTE)
 , v17     VARCHAR2 (10 BYTE)
 , v18     VARCHAR2 (10 BYTE)
 , v19     VARCHAR2 (10 BYTE)
 , v20     VARCHAR2 (10 BYTE)
 , v21     VARCHAR2 (10 BYTE)
 , v22     VARCHAR2 (10 BYTE)
 , v23     VARCHAR2 (10 BYTE)
 , v24     VARCHAR2 (10 BYTE)
 , v25     VARCHAR2 (10 BYTE)
 , v26     VARCHAR2 (10 BYTE)
 , v27     VARCHAR2 (10 BYTE)
 , v28     VARCHAR2 (10 BYTE)
 , v29     VARCHAR2 (10 BYTE)
 , v30     VARCHAR2 (10 BYTE)
 , v31     VARCHAR2 (10 BYTE)
 , v32     VARCHAR2 (10 BYTE)
 , v33     VARCHAR2 (10 BYTE)
 , v34     VARCHAR2 (10 BYTE)
 , v35     VARCHAR2 (10 BYTE)
 , v36     VARCHAR2 (10 BYTE)
 , v37     VARCHAR2 (10 BYTE)
 , v38     VARCHAR2 (10 BYTE)
 , v39     VARCHAR2 (10 BYTE)
 , v40     VARCHAR2 (10 BYTE)
 , v41     VARCHAR2 (10 BYTE)
 , v42     VARCHAR2 (10 BYTE)
 , v43     VARCHAR2 (10 BYTE)
 , v44     VARCHAR2 (10 BYTE)
 , v45     VARCHAR2 (10 BYTE)
 , v46     VARCHAR2 (10 BYTE)
 , v47     VARCHAR2 (10 BYTE)
 , v48     VARCHAR2 (10 BYTE)
 , v49     VARCHAR2 (10 BYTE)
 , v50     VARCHAR2 (10 BYTE)
 , v51     VARCHAR2 (10 BYTE)
 , v52     VARCHAR2 (10 BYTE)
 , v53     VARCHAR2 (10 BYTE)
 , v54     VARCHAR2 (10 BYTE)
 , v55     VARCHAR2 (10 BYTE)
 , v56     VARCHAR2 (10 BYTE)
 , v57     VARCHAR2 (10 BYTE)
 , v58     VARCHAR2 (10 BYTE)
 , v59     VARCHAR2 (10 BYTE)
 , v60     VARCHAR2 (10 BYTE)
 , v61     VARCHAR2 (10 BYTE)
 , v62     VARCHAR2 (10 BYTE)
 , v63     VARCHAR2 (10 BYTE)
 , v64     VARCHAR2 (10 BYTE)
 , v65     VARCHAR2 (10 BYTE)
 , v66     VARCHAR2 (10 BYTE)
 , v67     VARCHAR2 (10 BYTE)
 , v68     VARCHAR2 (10 BYTE)
 , v69     VARCHAR2 (10 BYTE)
 , v70     VARCHAR2 (10 BYTE)
 , v71     VARCHAR2 (10 BYTE)
 , v72     VARCHAR2 (10 BYTE)
 , v73     VARCHAR2 (10 BYTE)
 , v74     VARCHAR2 (10 BYTE)
 , v75     VARCHAR2 (10 BYTE)
 , v76     VARCHAR2 (10 BYTE)
 , v77     VARCHAR2 (10 BYTE)
 , v78     VARCHAR2 (10 BYTE)
 , v79     VARCHAR2 (10 BYTE)
 , v80     VARCHAR2 (10 BYTE)
 , v81     VARCHAR2 (10 BYTE)
 , v82     VARCHAR2 (10 BYTE)
 , v83     VARCHAR2 (10 BYTE)
 , v84     VARCHAR2 (10 BYTE)
 , v85     VARCHAR2 (10 BYTE)
 , v86     VARCHAR2 (10 BYTE)
 , v87     VARCHAR2 (10 BYTE)
 , v88     VARCHAR2 (10 BYTE)
 , v89     VARCHAR2 (10 BYTE)
 , v90     VARCHAR2 (10 BYTE)
 , v91     VARCHAR2 (10 BYTE)
 , v92     VARCHAR2 (10 BYTE)
 , v93     VARCHAR2 (10 BYTE)
 , v94     VARCHAR2 (10 BYTE)
 , v95     VARCHAR2 (10 BYTE)
 , v96     VARCHAR2 (10 BYTE)
 , v97     VARCHAR2 (10 BYTE)
 , v98     VARCHAR2 (10 BYTE)
 , v99     VARCHAR2 (10 BYTE)
 , v100    VARCHAR2 (10 BYTE)
 , v101    VARCHAR2 (10 BYTE)
 , v102    VARCHAR2 (10 BYTE)
 , v103    VARCHAR2 (10 BYTE)
 , v104    VARCHAR2 (10 BYTE)
 , v105    VARCHAR2 (10 BYTE)
 , v106    VARCHAR2 (10 BYTE)
 , v107    VARCHAR2 (10 BYTE)
 , v108    VARCHAR2 (10 BYTE)
 , v109    VARCHAR2 (10 BYTE)
 , v110    VARCHAR2 (10 BYTE)
 , v111    VARCHAR2 (10 BYTE)
 , v112    VARCHAR2 (10 BYTE)
 , v113    VARCHAR2 (10 BYTE)
 , v114    VARCHAR2 (10 BYTE)
 , v115    VARCHAR2 (10 BYTE)
 , v116    VARCHAR2 (10 BYTE)
 , v117    VARCHAR2 (10 BYTE)
 , v118    VARCHAR2 (10 BYTE)
 , v119    VARCHAR2 (10 BYTE)
 , v120    VARCHAR2 (10 BYTE)
 , v121    VARCHAR2 (10 BYTE)
 , v122    VARCHAR2 (10 BYTE)
 , v123    VARCHAR2 (10 BYTE)
 , v124    VARCHAR2 (10 BYTE)
 , v125    VARCHAR2 (10 BYTE)
 , v126    VARCHAR2 (10 BYTE)
 , v127    VARCHAR2 (10 BYTE)
 , v128    VARCHAR2 (10 BYTE)
 , v129    VARCHAR2 (10 BYTE)
 , v130    VARCHAR2 (10 BYTE)
 , v131    VARCHAR2 (10 BYTE)
 , v132    VARCHAR2 (10 BYTE)
 , v133    VARCHAR2 (10 BYTE)
 , v134    VARCHAR2 (10 BYTE)
 , v135    VARCHAR2 (10 BYTE)
 , v136    VARCHAR2 (10 BYTE)
 , v137    VARCHAR2 (10 BYTE)
 , v138    VARCHAR2 (10 BYTE)
 , v139    VARCHAR2 (10 BYTE)
 , v140    VARCHAR2 (10 BYTE)
 , v141    VARCHAR2 (10 BYTE)
 , v142    VARCHAR2 (10 BYTE)
 , v143    VARCHAR2 (10 BYTE)
 , v144    VARCHAR2 (10 BYTE)
 , v145    VARCHAR2 (10 BYTE)
 , v146    VARCHAR2 (10 BYTE)
 , v147    VARCHAR2 (10 BYTE)
 , v148    VARCHAR2 (10 BYTE)
 , v149    VARCHAR2 (10 BYTE)
 , v150    VARCHAR2 (10 BYTE)
 , v151    VARCHAR2 (10 BYTE)
 , v152    VARCHAR2 (10 BYTE)
 , v153    VARCHAR2 (10 BYTE)
 , v154    VARCHAR2 (10 BYTE)
 , v155    VARCHAR2 (10 BYTE)
 , v156    VARCHAR2 (10 BYTE)
 , v157    VARCHAR2 (10 BYTE)
 , v158    VARCHAR2 (10 BYTE)
 , v159    VARCHAR2 (10 BYTE)
 , v160    VARCHAR2 (10 BYTE)
 , v161    VARCHAR2 (10 BYTE)
 , v162    VARCHAR2 (10 BYTE)
 , v163    VARCHAR2 (10 BYTE)
 , v164    VARCHAR2 (10 BYTE)
 , v165    VARCHAR2 (10 BYTE)
 , v166    VARCHAR2 (10 BYTE)
 , v167    VARCHAR2 (10 BYTE)
 , v168    VARCHAR2 (10 BYTE)
 , v169    VARCHAR2 (10 BYTE)
 , v170    VARCHAR2 (10 BYTE)
 , v171    VARCHAR2 (10 BYTE)
 , v172    VARCHAR2 (10 BYTE)
 , v173    VARCHAR2 (10 BYTE)
 , v174    VARCHAR2 (10 BYTE)
 , v175    VARCHAR2 (10 BYTE)
 , v176    VARCHAR2 (10 BYTE)
 , v177    VARCHAR2 (10 BYTE)
 , v178    VARCHAR2 (10 BYTE)
 , v179    VARCHAR2 (10 BYTE)
 , v180    VARCHAR2 (10 BYTE)
 , v181    VARCHAR2 (10 BYTE)
 , v182    VARCHAR2 (10 BYTE)
 , v183    VARCHAR2 (10 BYTE)
 , v184    VARCHAR2 (10 BYTE)
 , v185    VARCHAR2 (10 BYTE)
 , v186    VARCHAR2 (10 BYTE)
 , v187    VARCHAR2 (10 BYTE)
 , v188    VARCHAR2 (10 BYTE)
 , v189    VARCHAR2 (10 BYTE)
 , v190    VARCHAR2 (10 BYTE)
 , v191    VARCHAR2 (10 BYTE)
 , v192    VARCHAR2 (10 BYTE)
 , v193    VARCHAR2 (10 BYTE)
 , v194    VARCHAR2 (10 BYTE)
 , v195    VARCHAR2 (10 BYTE)
 , v196    VARCHAR2 (10 BYTE)
 , v197    VARCHAR2 (10 BYTE)
 , v198    VARCHAR2 (10 BYTE)
 , v199    VARCHAR2 (10 BYTE)
 , v200    VARCHAR2 (10 BYTE)
 , v201    VARCHAR2 (10 BYTE)
 , v202    VARCHAR2 (10 BYTE)
 , v203    VARCHAR2 (10 BYTE)
 , v204    VARCHAR2 (10 BYTE)
 , v205    VARCHAR2 (10 BYTE)
 , v206    VARCHAR2 (10 BYTE)
 , v207    VARCHAR2 (10 BYTE)
 , v208    VARCHAR2 (10 BYTE)
 , v209    VARCHAR2 (10 BYTE)
 , v210    VARCHAR2 (10 BYTE)
 , v211    VARCHAR2 (10 BYTE)
 , v212    VARCHAR2 (10 BYTE)
 , v213    VARCHAR2 (10 BYTE)
 , v214    VARCHAR2 (10 BYTE)
 , v215    VARCHAR2 (10 BYTE)
 , v216    VARCHAR2 (10 BYTE)
 , v217    VARCHAR2 (10 BYTE)
 , v218    VARCHAR2 (10 BYTE)
 , v219    VARCHAR2 (10 BYTE)
 , v220    VARCHAR2 (10 BYTE)
 , v221    VARCHAR2 (10 BYTE)
 , v222    VARCHAR2 (10 BYTE)
 , v223    VARCHAR2 (10 BYTE)
 , v224    VARCHAR2 (10 BYTE)
 , v225    VARCHAR2 (10 BYTE)
 , v226    VARCHAR2 (10 BYTE)
 , v227    VARCHAR2 (10 BYTE)
 , v228    VARCHAR2 (10 BYTE)
 , v229    VARCHAR2 (10 BYTE)
 , v230    VARCHAR2 (10 BYTE)
 , v231    VARCHAR2 (10 BYTE)
 , v232    VARCHAR2 (10 BYTE)
 , v233    VARCHAR2 (10 BYTE)
 , v234    VARCHAR2 (10 BYTE)
 , v235    VARCHAR2 (10 BYTE)
 , v236    VARCHAR2 (10 BYTE)
 , v237    VARCHAR2 (10 BYTE)
 , v238    VARCHAR2 (10 BYTE)
 , v239    VARCHAR2 (10 BYTE)
 , v240    VARCHAR2 (10 BYTE)
 , v241    VARCHAR2 (10 BYTE)
 , v242    VARCHAR2 (10 BYTE)
 , v243    VARCHAR2 (10 BYTE)
 , v244    VARCHAR2 (10 BYTE)
 , v245    VARCHAR2 (10 BYTE)
 , v246    VARCHAR2 (10 BYTE)
 , v247    VARCHAR2 (10 BYTE)
 , v248    VARCHAR2 (10 BYTE)
 , v249    VARCHAR2 (10 BYTE)
 , v250    VARCHAR2 (10 BYTE)
 , v251    VARCHAR2 (10 BYTE)
 , v252    VARCHAR2 (10 BYTE)
 , v253    VARCHAR2 (10 BYTE)
 , v254    VARCHAR2 (10 BYTE)
 , v255    VARCHAR2 (10 BYTE)
 , v256    VARCHAR2 (10 BYTE)
 , v257    VARCHAR2 (10 BYTE)
 , v258    VARCHAR2 (10 BYTE)
 , v259    VARCHAR2 (10 BYTE)
 , v260    VARCHAR2 (10 BYTE)
 , v261    VARCHAR2 (10 BYTE)
 , v262    VARCHAR2 (10 BYTE)
 , v263    VARCHAR2 (10 BYTE)
 , v264    VARCHAR2 (10 BYTE)
 , v265    VARCHAR2 (10 BYTE)
 , v266    VARCHAR2 (10 BYTE)
 , v267    VARCHAR2 (10 BYTE)
 , v268    VARCHAR2 (10 BYTE)
 , v269    VARCHAR2 (10 BYTE)
 , v270    VARCHAR2 (10 BYTE)
 , v271    VARCHAR2 (10 BYTE)
 , v272    VARCHAR2 (10 BYTE)
 , v273    VARCHAR2 (10 BYTE)
 , v274    VARCHAR2 (10 BYTE)
 , v275    VARCHAR2 (10 BYTE)
 , v276    VARCHAR2 (10 BYTE)
 , v277    VARCHAR2 (10 BYTE)
 , v278    VARCHAR2 (10 BYTE)
 , v279    VARCHAR2 (10 BYTE)
 , v280    VARCHAR2 (10 BYTE)
 , v281    VARCHAR2 (10 BYTE)
 , v282    VARCHAR2 (10 BYTE)
 , v283    VARCHAR2 (10 BYTE)
 , v284    VARCHAR2 (10 BYTE)
 , v285    VARCHAR2 (10 BYTE)
 , v286    VARCHAR2 (10 BYTE)
 , v287    VARCHAR2 (10 BYTE)
 , v288    VARCHAR2 (10 BYTE)
 , v289    VARCHAR2 (10 BYTE)
 , v290    VARCHAR2 (10 BYTE)
 , v291    VARCHAR2 (10 BYTE)
 , v292    VARCHAR2 (10 BYTE)
 , v293    VARCHAR2 (10 BYTE)
 , v294    VARCHAR2 (10 BYTE)
 , v295    VARCHAR2 (10 BYTE)
 , v296    VARCHAR2 (10 BYTE)
 , v297    VARCHAR2 (10 BYTE)
 , v298    VARCHAR2 (10 BYTE)
 , v299    VARCHAR2 (10 BYTE)
 , v300    VARCHAR2 (10 BYTE)
 , v301    VARCHAR2 (10 BYTE)
 , v302    VARCHAR2 (10 BYTE)
 , v303    VARCHAR2 (10 BYTE)
 , v304    VARCHAR2 (10 BYTE)
 , v305    VARCHAR2 (10 BYTE)
 , v306    VARCHAR2 (10 BYTE)
 , v307    VARCHAR2 (10 BYTE)
 , v308    VARCHAR2 (10 BYTE)
 , v309    VARCHAR2 (10 BYTE)
 , v310    VARCHAR2 (10 BYTE)
 , v311    VARCHAR2 (10 BYTE)
 , v312    VARCHAR2 (10 BYTE)
 , v313    VARCHAR2 (10 BYTE)
 , v314    VARCHAR2 (10 BYTE)
 , v315    VARCHAR2 (10 BYTE)
 , v316    VARCHAR2 (10 BYTE)
 , v317    VARCHAR2 (10 BYTE)
 , v318    VARCHAR2 (10 BYTE)
 , v319    VARCHAR2 (10 BYTE)
 , v320    VARCHAR2 (10 BYTE)
 , v321    VARCHAR2 (10 BYTE)
 , v322    VARCHAR2 (10 BYTE)
 , v323    VARCHAR2 (10 BYTE)
 , v324    VARCHAR2 (10 BYTE)
 , v325    VARCHAR2 (10 BYTE)
 , v326    VARCHAR2 (10 BYTE)
 , v327    VARCHAR2 (10 BYTE)
 , v328    VARCHAR2 (10 BYTE)
 , v329    VARCHAR2 (10 BYTE)
 , v330    VARCHAR2 (10 BYTE)
 , v331    VARCHAR2 (10 BYTE)
 , v332    VARCHAR2 (10 BYTE)
 , v333    VARCHAR2 (10 BYTE)
 , v334    VARCHAR2 (10 BYTE)
 , v335    VARCHAR2 (10 BYTE)
 , v336    VARCHAR2 (10 BYTE)
 , v337    VARCHAR2 (10 BYTE)
 , v338    VARCHAR2 (10 BYTE)
 , v339    VARCHAR2 (10 BYTE)
 , v340    VARCHAR2 (10 BYTE)
 , v341    VARCHAR2 (10 BYTE)
 , v342    VARCHAR2 (10 BYTE)
 , v343    VARCHAR2 (10 BYTE)
 , v344    VARCHAR2 (10 BYTE)
 , v345    VARCHAR2 (10 BYTE)
 , v346    VARCHAR2 (10 BYTE)
 , v347    VARCHAR2 (10 BYTE)
 , v348    VARCHAR2 (10 BYTE)
 , v349    VARCHAR2 (10 BYTE)
 , v350    VARCHAR2 (10 BYTE)
 , v351    VARCHAR2 (10 BYTE)
 , v352    VARCHAR2 (10 BYTE)
 , v353    VARCHAR2 (10 BYTE)
 , v354    VARCHAR2 (10 BYTE)
 , v355    VARCHAR2 (10 BYTE)
 , v356    VARCHAR2 (10 BYTE)
 , v357    VARCHAR2 (10 BYTE)
 , v358    VARCHAR2 (10 BYTE)
 , v359    VARCHAR2 (10 BYTE)
 , v360    VARCHAR2 (10 BYTE)
 , v361    VARCHAR2 (10 BYTE)
 , v362    VARCHAR2 (10 BYTE)
 , v363    VARCHAR2 (10 BYTE)
 , v364    VARCHAR2 (10 BYTE)
 , v365    VARCHAR2 (10 BYTE)
 , v366    VARCHAR2 (10 BYTE)
 , v367    VARCHAR2 (10 BYTE)
 , v368    VARCHAR2 (10 BYTE)
 , v369    VARCHAR2 (10 BYTE)
 , v370    VARCHAR2 (10 BYTE)
 , v371    VARCHAR2 (10 BYTE)
 , v372    VARCHAR2 (10 BYTE)
 , v373    VARCHAR2 (10 BYTE)
 , v374    VARCHAR2 (10 BYTE)
 , v375    VARCHAR2 (10 BYTE)
 , v376    VARCHAR2 (10 BYTE)
 , v377    VARCHAR2 (10 BYTE)
 , v378    VARCHAR2 (10 BYTE)
 , v379    VARCHAR2 (10 BYTE)
 , v380    VARCHAR2 (10 BYTE)
 , v381    VARCHAR2 (10 BYTE)
 , v382    VARCHAR2 (10 BYTE)
 , v383    VARCHAR2 (10 BYTE)
 , v384    VARCHAR2 (10 BYTE)
 , v385    VARCHAR2 (10 BYTE)
 , v386    VARCHAR2 (10 BYTE)
 , v387    VARCHAR2 (10 BYTE)
 , v388    VARCHAR2 (10 BYTE)
 , v389    VARCHAR2 (10 BYTE)
 , v390    VARCHAR2 (10 BYTE)
 , v391    VARCHAR2 (10 BYTE)
 , v392    VARCHAR2 (10 BYTE)
 , v393    VARCHAR2 (10 BYTE)
 , v394    VARCHAR2 (10 BYTE)
 , v395    VARCHAR2 (10 BYTE)
 , v396    VARCHAR2 (10 BYTE)
 , v397    VARCHAR2 (10 BYTE)
 , v398    VARCHAR2 (10 BYTE)
 , v399    VARCHAR2 (10 BYTE)
 , v400    VARCHAR2 (10 BYTE)
 , v401    VARCHAR2 (10 BYTE)
 , v402    VARCHAR2 (10 BYTE)
 , v403    VARCHAR2 (10 BYTE)
 , v404    VARCHAR2 (10 BYTE)
 , v405    VARCHAR2 (10 BYTE)
 , v406    VARCHAR2 (10 BYTE)
 , v407    VARCHAR2 (10 BYTE)
 , v408    VARCHAR2 (10 BYTE)
 , v409    VARCHAR2 (10 BYTE)
 , v410    VARCHAR2 (10 BYTE)
 , v411    VARCHAR2 (10 BYTE)
 , v412    VARCHAR2 (10 BYTE)
 , v413    VARCHAR2 (10 BYTE)
 , v414    VARCHAR2 (10 BYTE)
 , v415    VARCHAR2 (10 BYTE)
 , v416    VARCHAR2 (10 BYTE)
 , v417    VARCHAR2 (10 BYTE)
 , v418    VARCHAR2 (10 BYTE)
 , v419    VARCHAR2 (10 BYTE)
 , v420    VARCHAR2 (10 BYTE)
 , v421    VARCHAR2 (10 BYTE)
 , v422    VARCHAR2 (10 BYTE)
 , v423    VARCHAR2 (10 BYTE)
 , v424    VARCHAR2 (10 BYTE)
 , v425    VARCHAR2 (10 BYTE)
 , v426    VARCHAR2 (10 BYTE)
 , v427    VARCHAR2 (10 BYTE)
 , v428    VARCHAR2 (10 BYTE)
 , v429    VARCHAR2 (10 BYTE)
 , v430    VARCHAR2 (10 BYTE)
 , v431    VARCHAR2 (10 BYTE)
 , v432    VARCHAR2 (10 BYTE)
 , v433    VARCHAR2 (10 BYTE)
 , v434    VARCHAR2 (10 BYTE)
 , v435    VARCHAR2 (10 BYTE)
 , v436    VARCHAR2 (10 BYTE)
 , v437    VARCHAR2 (10 BYTE)
 , v438    VARCHAR2 (10 BYTE)
 , v439    VARCHAR2 (10 BYTE)
 , v440    VARCHAR2 (10 BYTE)
 , v441    VARCHAR2 (10 BYTE)
 , v442    VARCHAR2 (10 BYTE)
 , v443    VARCHAR2 (10 BYTE)
 , v444    VARCHAR2 (10 BYTE)
 , v445    VARCHAR2 (10 BYTE)
 , v446    VARCHAR2 (10 BYTE)
 , v447    VARCHAR2 (10 BYTE)
 , v448    VARCHAR2 (10 BYTE)
 , v449    VARCHAR2 (10 BYTE)
 , v450    VARCHAR2 (10 BYTE)
 , v451    VARCHAR2 (10 BYTE)
 , v452    VARCHAR2 (10 BYTE)
 , v453    VARCHAR2 (10 BYTE)
 , v454    VARCHAR2 (10 BYTE)
 , v455    VARCHAR2 (10 BYTE)
 , v456    VARCHAR2 (10 BYTE)
 , v457    VARCHAR2 (10 BYTE)
 , v458    VARCHAR2 (10 BYTE)
 , v459    VARCHAR2 (10 BYTE)
 , v460    VARCHAR2 (10 BYTE)
 , v461    VARCHAR2 (10 BYTE)
 , v462    VARCHAR2 (10 BYTE)
 , v463    VARCHAR2 (10 BYTE)
 , v464    VARCHAR2 (10 BYTE)
 , v465    VARCHAR2 (10 BYTE)
 , v466    VARCHAR2 (10 BYTE)
 , v467    VARCHAR2 (10 BYTE)
 , v468    VARCHAR2 (10 BYTE)
 , v469    VARCHAR2 (10 BYTE)
 , v470    VARCHAR2 (10 BYTE)
 , v471    VARCHAR2 (10 BYTE)
 , v472    VARCHAR2 (10 BYTE)
 , v473    VARCHAR2 (10 BYTE)
 , v474    VARCHAR2 (10 BYTE)
 , v475    VARCHAR2 (10 BYTE)
 , v476    VARCHAR2 (10 BYTE)
 , v477    VARCHAR2 (10 BYTE)
 , v478    VARCHAR2 (10 BYTE)
 , v479    VARCHAR2 (10 BYTE)
 , v480    VARCHAR2 (10 BYTE)
 , v481    VARCHAR2 (10 BYTE)
 , v482    VARCHAR2 (10 BYTE)
 , v483    VARCHAR2 (10 BYTE)
 , v484    VARCHAR2 (10 BYTE)
 , v485    VARCHAR2 (10 BYTE)
 , v486    VARCHAR2 (10 BYTE)
 , v487    VARCHAR2 (10 BYTE)
 , v488    VARCHAR2 (10 BYTE)
 , v489    VARCHAR2 (10 BYTE)
 , v490    VARCHAR2 (10 BYTE)
 , v491    VARCHAR2 (10 BYTE)
 , v492    VARCHAR2 (10 BYTE)
 , v493    VARCHAR2 (10 BYTE)
 , v494    VARCHAR2 (10 BYTE)
 , v495    VARCHAR2 (10 BYTE)
 , v496    VARCHAR2 (10 BYTE)
 , v497    VARCHAR2 (10 BYTE)
 , v498    VARCHAR2 (10 BYTE)
 , v499    VARCHAR2 (10 BYTE)
 , v500    VARCHAR2 (10 BYTE)
 , v501    VARCHAR2 (10 BYTE)
 , v502    VARCHAR2 (10 BYTE)
 , v503    VARCHAR2 (10 BYTE)
 , v504    VARCHAR2 (10 BYTE)
 , v505    VARCHAR2 (10 BYTE)
 , v506    VARCHAR2 (10 BYTE)
 , v507    VARCHAR2 (10 BYTE)
 , v508    VARCHAR2 (10 BYTE)
 , v509    VARCHAR2 (10 BYTE)
 , v510    VARCHAR2 (10 BYTE)
 , v511    VARCHAR2 (10 BYTE)
 , v512    VARCHAR2 (10 BYTE)
 , v513    VARCHAR2 (10 BYTE)
 , v514    VARCHAR2 (10 BYTE)
 , v515    VARCHAR2 (10 BYTE)
 , v516    VARCHAR2 (10 BYTE)
 , v517    VARCHAR2 (10 BYTE)
 , v518    VARCHAR2 (10 BYTE)
 , v519    VARCHAR2 (10 BYTE)
 , v520    VARCHAR2 (10 BYTE)
 , v521    VARCHAR2 (10 BYTE)
 , v522    VARCHAR2 (10 BYTE)
 , v523    VARCHAR2 (10 BYTE)
 , v524    VARCHAR2 (10 BYTE)
 , v525    VARCHAR2 (10 BYTE)
 , v526    VARCHAR2 (10 BYTE)
 , v527    VARCHAR2 (10 BYTE)
 , v528    VARCHAR2 (10 BYTE)
 , v529    VARCHAR2 (10 BYTE)
 , v530    VARCHAR2 (10 BYTE)
 , v531    VARCHAR2 (10 BYTE)
 , v532    VARCHAR2 (10 BYTE)
 , v533    VARCHAR2 (10 BYTE)
 , v534    VARCHAR2 (10 BYTE)
 , v535    VARCHAR2 (10 BYTE)
 , v536    VARCHAR2 (10 BYTE)
 , v537    VARCHAR2 (10 BYTE)
 , v538    VARCHAR2 (10 BYTE)
 , v539    VARCHAR2 (10 BYTE)
 , v540    VARCHAR2 (10 BYTE)
 , v541    VARCHAR2 (10 BYTE)
 , v542    VARCHAR2 (10 BYTE)
 , v543    VARCHAR2 (10 BYTE)
 , v544    VARCHAR2 (10 BYTE)
 , v545    VARCHAR2 (10 BYTE)
 , v546    VARCHAR2 (10 BYTE)
 , v547    VARCHAR2 (10 BYTE)
 , v548    VARCHAR2 (10 BYTE)
 , v549    VARCHAR2 (10 BYTE)
 , v550    VARCHAR2 (10 BYTE)
 , v551    VARCHAR2 (10 BYTE)
 , v552    VARCHAR2 (10 BYTE)
 , v553    VARCHAR2 (10 BYTE)
 , v554    VARCHAR2 (10 BYTE)
 , v555    VARCHAR2 (10 BYTE)
 , v556    VARCHAR2 (10 BYTE)
 , v557    VARCHAR2 (10 BYTE)
 , v558    VARCHAR2 (10 BYTE)
 , v559    VARCHAR2 (10 BYTE)
 , v560    VARCHAR2 (10 BYTE)
 , v561    VARCHAR2 (10 BYTE)
 , v562    VARCHAR2 (10 BYTE)
 , v563    VARCHAR2 (10 BYTE)
 , v564    VARCHAR2 (10 BYTE)
 , v565    VARCHAR2 (10 BYTE)
 , v566    VARCHAR2 (10 BYTE)
 , v567    VARCHAR2 (10 BYTE)
 , v568    VARCHAR2 (10 BYTE)
 , v569    VARCHAR2 (10 BYTE)
 , v570    VARCHAR2 (10 BYTE)
 , v571    VARCHAR2 (10 BYTE)
 , v572    VARCHAR2 (10 BYTE)
 , v573    VARCHAR2 (10 BYTE)
 , v574    VARCHAR2 (10 BYTE)
 , v575    VARCHAR2 (10 BYTE)
 , v576    VARCHAR2 (10 BYTE)
 , v577    VARCHAR2 (10 BYTE)
 , v578    VARCHAR2 (10 BYTE)
 , v579    VARCHAR2 (10 BYTE)
 , v580    VARCHAR2 (10 BYTE)
 , v581    VARCHAR2 (10 BYTE)
 , v582    VARCHAR2 (10 BYTE)
 , v583    VARCHAR2 (10 BYTE)
 , v584    VARCHAR2 (10 BYTE)
 , v585    VARCHAR2 (10 BYTE)
 , v586    VARCHAR2 (10 BYTE)
 , v587    VARCHAR2 (10 BYTE)
 , v588    VARCHAR2 (10 BYTE)
 , v589    VARCHAR2 (10 BYTE)
 , v590    VARCHAR2 (10 BYTE)
 , v591    VARCHAR2 (10 BYTE)
 , v592    VARCHAR2 (10 BYTE)
 , v593    VARCHAR2 (10 BYTE)
 , v594    VARCHAR2 (10 BYTE)
 , v595    VARCHAR2 (10 BYTE)
 , v596    VARCHAR2 (10 BYTE)
 , v597    VARCHAR2 (10 BYTE)
 , v598    VARCHAR2 (10 BYTE)
 , v599    VARCHAR2 (10 BYTE)
 , v600    VARCHAR2 (10 BYTE)
 , v601    VARCHAR2 (10 BYTE)
 , v602    VARCHAR2 (10 BYTE)
 , v603    VARCHAR2 (10 BYTE)
 , v604    VARCHAR2 (10 BYTE)
 , v605    VARCHAR2 (10 BYTE)
 , v606    VARCHAR2 (10 BYTE)
 , v607    VARCHAR2 (10 BYTE)
 , v608    VARCHAR2 (10 BYTE)
 , v609    VARCHAR2 (10 BYTE)
 , v610    VARCHAR2 (10 BYTE)
 , v611    VARCHAR2 (10 BYTE)
 , v612    VARCHAR2 (10 BYTE)
 , v613    VARCHAR2 (10 BYTE)
 , v614    VARCHAR2 (10 BYTE)
 , v615    VARCHAR2 (10 BYTE)
 , v616    VARCHAR2 (10 BYTE)
 , v617    VARCHAR2 (10 BYTE)
 , v618    VARCHAR2 (10 BYTE)
 , v619    VARCHAR2 (10 BYTE)
 , v620    VARCHAR2 (10 BYTE)
 , v621    VARCHAR2 (10 BYTE)
 , v622    VARCHAR2 (10 BYTE)
 , v623    VARCHAR2 (10 BYTE)
 , v624    VARCHAR2 (10 BYTE)
 , v625    VARCHAR2 (10 BYTE)
 , v626    VARCHAR2 (10 BYTE)
 , v627    VARCHAR2 (10 BYTE)
 , v628    VARCHAR2 (10 BYTE)
 , v629    VARCHAR2 (10 BYTE)
 , v630    VARCHAR2 (10 BYTE)
 , v631    VARCHAR2 (10 BYTE)
 , v632    VARCHAR2 (10 BYTE)
 , v633    VARCHAR2 (10 BYTE)
 , v634    VARCHAR2 (10 BYTE)
 , v635    VARCHAR2 (10 BYTE)
 , v636    VARCHAR2 (10 BYTE)
 , v637    VARCHAR2 (10 BYTE)
 , v638    VARCHAR2 (10 BYTE)
 , v639    VARCHAR2 (10 BYTE)
 , v640    VARCHAR2 (10 BYTE)
 , v641    VARCHAR2 (10 BYTE)
 , v642    VARCHAR2 (10 BYTE)
 , v643    VARCHAR2 (10 BYTE)
 , v644    VARCHAR2 (10 BYTE)
 , v645    VARCHAR2 (10 BYTE)
 , v646    VARCHAR2 (10 BYTE)
 , v647    VARCHAR2 (10 BYTE)
 , v648    VARCHAR2 (10 BYTE)
 , v649    VARCHAR2 (10 BYTE)
 , v650    VARCHAR2 (10 BYTE)
 , v651    VARCHAR2 (10 BYTE)
 , v652    VARCHAR2 (10 BYTE)
 , v653    VARCHAR2 (10 BYTE)
 , v654    VARCHAR2 (10 BYTE)
 , v655    VARCHAR2 (10 BYTE)
 , v656    VARCHAR2 (10 BYTE)
 , v657    VARCHAR2 (10 BYTE)
 , v658    VARCHAR2 (10 BYTE)
 , v659    VARCHAR2 (10 BYTE)
 , v660    VARCHAR2 (10 BYTE)
 , v661    VARCHAR2 (10 BYTE)
 , v662    VARCHAR2 (10 BYTE)
 , v663    VARCHAR2 (10 BYTE)
 , v664    VARCHAR2 (10 BYTE)
 , v665    VARCHAR2 (10 BYTE)
 , v666    VARCHAR2 (10 BYTE)
 , v667    VARCHAR2 (10 BYTE)
 , v668    VARCHAR2 (10 BYTE)
 , v669    VARCHAR2 (10 BYTE)
 , v670    VARCHAR2 (10 BYTE)
 , v671    VARCHAR2 (10 BYTE)
 , v672    VARCHAR2 (10 BYTE)
 , v673    VARCHAR2 (10 BYTE)
 , v674    VARCHAR2 (10 BYTE)
 , v675    VARCHAR2 (10 BYTE)
 , v676    VARCHAR2 (10 BYTE)
 , v677    VARCHAR2 (10 BYTE)
 , v678    VARCHAR2 (10 BYTE)
 , v679    VARCHAR2 (10 BYTE)
 , v680    VARCHAR2 (10 BYTE)
 , v681    VARCHAR2 (10 BYTE)
 , v682    VARCHAR2 (10 BYTE)
 , v683    VARCHAR2 (10 BYTE)
 , v684    VARCHAR2 (10 BYTE)
 , v685    VARCHAR2 (10 BYTE)
 , v686    VARCHAR2 (10 BYTE)
 , v687    VARCHAR2 (10 BYTE)
 , v688    VARCHAR2 (10 BYTE)
 , v689    VARCHAR2 (10 BYTE)
 , v690    VARCHAR2 (10 BYTE)
 , v691    VARCHAR2 (10 BYTE)
 , v692    VARCHAR2 (10 BYTE)
 , v693    VARCHAR2 (10 BYTE)
 , v694    VARCHAR2 (10 BYTE)
 , v695    VARCHAR2 (10 BYTE)
 , v696    VARCHAR2 (10 BYTE)
 , v697    VARCHAR2 (10 BYTE)
 , v698    VARCHAR2 (10 BYTE)
 , v699    VARCHAR2 (10 BYTE)
 , v700    VARCHAR2 (10 BYTE)
 , v701    VARCHAR2 (10 BYTE)
 , v702    VARCHAR2 (10 BYTE)
 , v703    VARCHAR2 (10 BYTE)
 , v704    VARCHAR2 (10 BYTE)
 , v705    VARCHAR2 (10 BYTE)
 , v706    VARCHAR2 (10 BYTE)
 , v707    VARCHAR2 (10 BYTE)
 , v708    VARCHAR2 (10 BYTE)
 , v709    VARCHAR2 (10 BYTE)
 , v710    VARCHAR2 (10 BYTE)
 , v711    VARCHAR2 (10 BYTE)
 , v712    VARCHAR2 (10 BYTE)
 , v713    VARCHAR2 (10 BYTE)
 , v714    VARCHAR2 (10 BYTE)
 , v715    VARCHAR2 (10 BYTE)
 , v716    VARCHAR2 (10 BYTE)
 , v717    VARCHAR2 (10 BYTE)
 , v718    VARCHAR2 (10 BYTE)
 , v719    VARCHAR2 (10 BYTE)
 , v720    VARCHAR2 (10 BYTE)
 , v721    VARCHAR2 (10 BYTE)
 , v722    VARCHAR2 (10 BYTE)
 , v723    VARCHAR2 (10 BYTE)
 , v724    VARCHAR2 (10 BYTE)
 , v725    VARCHAR2 (10 BYTE)
 , v726    VARCHAR2 (10 BYTE)
 , v727    VARCHAR2 (10 BYTE)
 , v728    VARCHAR2 (10 BYTE)
 , v729    VARCHAR2 (10 BYTE)
 , v730    VARCHAR2 (10 BYTE)
 , v731    VARCHAR2 (10 BYTE)
 , v732    VARCHAR2 (10 BYTE)
 , v733    VARCHAR2 (10 BYTE)
 , v734    VARCHAR2 (10 BYTE)
 , v735    VARCHAR2 (10 BYTE)
 , v736    VARCHAR2 (10 BYTE)
 , v737    VARCHAR2 (10 BYTE)
 , v738    VARCHAR2 (10 BYTE)
 , v739    VARCHAR2 (10 BYTE)
 , v740    VARCHAR2 (10 BYTE)
 , v741    VARCHAR2 (10 BYTE)
 , v742    VARCHAR2 (10 BYTE)
 , v743    VARCHAR2 (10 BYTE)
 , v744    VARCHAR2 (10 BYTE)
 , v745    VARCHAR2 (10 BYTE)
 , v746    VARCHAR2 (10 BYTE)
 , v747    VARCHAR2 (10 BYTE)
 , v748    VARCHAR2 (10 BYTE)
 , v749    VARCHAR2 (10 BYTE)
 , v750    VARCHAR2 (10 BYTE)
 , v751    VARCHAR2 (10 BYTE)
 , v752    VARCHAR2 (10 BYTE)
 , v753    VARCHAR2 (10 BYTE)
 , v754    VARCHAR2 (10 BYTE)
 , v755    VARCHAR2 (10 BYTE)
 , v756    VARCHAR2 (10 BYTE)
 , v757    VARCHAR2 (10 BYTE)
 , v758    VARCHAR2 (10 BYTE)
 , v759    VARCHAR2 (10 BYTE)
 , v760    VARCHAR2 (10 BYTE)
 , v761    VARCHAR2 (10 BYTE)
 , v762    VARCHAR2 (10 BYTE)
 , v763    VARCHAR2 (10 BYTE)
 , v764    VARCHAR2 (10 BYTE)
 , v765    VARCHAR2 (10 BYTE)
 , v766    VARCHAR2 (10 BYTE)
 , v767    VARCHAR2 (10 BYTE)
 , v768    VARCHAR2 (10 BYTE)
 , v769    VARCHAR2 (10 BYTE)
 , v770    VARCHAR2 (10 BYTE)
 , v771    VARCHAR2 (10 BYTE)
 , v772    VARCHAR2 (10 BYTE)
 , v773    VARCHAR2 (10 BYTE)
 , v774    VARCHAR2 (10 BYTE)
 , v775    VARCHAR2 (10 BYTE)
 , v776    VARCHAR2 (10 BYTE)
 , v777    VARCHAR2 (10 BYTE)
 , v778    VARCHAR2 (10 BYTE)
 , v779    VARCHAR2 (10 BYTE)
 , v780    VARCHAR2 (10 BYTE)
 , v781    VARCHAR2 (10 BYTE)
 , v782    VARCHAR2 (10 BYTE)
 , v783    VARCHAR2 (10 BYTE)
 , v784    VARCHAR2 (10 BYTE)
 , v785    VARCHAR2 (10 BYTE)
 , v786    VARCHAR2 (10 BYTE)
 , v787    VARCHAR2 (10 BYTE)
 , v788    VARCHAR2 (10 BYTE)
 , v789    VARCHAR2 (10 BYTE)
 , v790    VARCHAR2 (10 BYTE)
 , v791    VARCHAR2 (10 BYTE)
 , v792    VARCHAR2 (10 BYTE)
 , v793    VARCHAR2 (10 BYTE)
 , v794    VARCHAR2 (10 BYTE)
 , v795    VARCHAR2 (10 BYTE)
 , v796    VARCHAR2 (10 BYTE)
 , v797    VARCHAR2 (10 BYTE)
 , v798    VARCHAR2 (10 BYTE)
 , v799    VARCHAR2 (10 BYTE)
 , v800    VARCHAR2 (10 BYTE)
 , v801    VARCHAR2 (10 BYTE)
 , v802    VARCHAR2 (10 BYTE)
 , v803    VARCHAR2 (10 BYTE)
 , v804    VARCHAR2 (10 BYTE)
 , v805    VARCHAR2 (10 BYTE)
 , v806    VARCHAR2 (10 BYTE)
 , v807    VARCHAR2 (10 BYTE)
 , v808    VARCHAR2 (10 BYTE)
 , v809    VARCHAR2 (10 BYTE)
 , v810    VARCHAR2 (10 BYTE)
 , v811    VARCHAR2 (10 BYTE)
 , v812    VARCHAR2 (10 BYTE)
 , v813    VARCHAR2 (10 BYTE)
 , v814    VARCHAR2 (10 BYTE)
 , v815    VARCHAR2 (10 BYTE)
 , v816    VARCHAR2 (10 BYTE)
 , v817    VARCHAR2 (10 BYTE)
 , v818    VARCHAR2 (10 BYTE)
 , v819    VARCHAR2 (10 BYTE)
 , v820    VARCHAR2 (10 BYTE)
 , v821    VARCHAR2 (10 BYTE)
 , v822    VARCHAR2 (10 BYTE)
 , v823    VARCHAR2 (10 BYTE)
 , v824    VARCHAR2 (10 BYTE)
 , v825    VARCHAR2 (10 BYTE)
 , v826    VARCHAR2 (10 BYTE)
 , v827    VARCHAR2 (10 BYTE)
 , v828    VARCHAR2 (10 BYTE)
 , v829    VARCHAR2 (10 BYTE)
 , v830    VARCHAR2 (10 BYTE)
 , v831    VARCHAR2 (10 BYTE)
 , v832    VARCHAR2 (10 BYTE)
 , v833    VARCHAR2 (10 BYTE)
 , v834    VARCHAR2 (10 BYTE)
 , v835    VARCHAR2 (10 BYTE)
 , v836    VARCHAR2 (10 BYTE)
 , v837    VARCHAR2 (10 BYTE)
 , v838    VARCHAR2 (10 BYTE)
 , v839    VARCHAR2 (10 BYTE)
 , v840    VARCHAR2 (10 BYTE)
 , v841    VARCHAR2 (10 BYTE)
 , v842    VARCHAR2 (10 BYTE)
 , v843    VARCHAR2 (10 BYTE)
 , v844    VARCHAR2 (10 BYTE)
 , v845    VARCHAR2 (10 BYTE)
 , v846    VARCHAR2 (10 BYTE)
 , v847    VARCHAR2 (10 BYTE)
 , v848    VARCHAR2 (10 BYTE)
 , v849    VARCHAR2 (10 BYTE)
 , v850    VARCHAR2 (10 BYTE)
 , v851    VARCHAR2 (10 BYTE)
 , v852    VARCHAR2 (10 BYTE)
 , v853    VARCHAR2 (10 BYTE)
 , v854    VARCHAR2 (10 BYTE)
 , v855    VARCHAR2 (10 BYTE)
 , v856    VARCHAR2 (10 BYTE)
 , v857    VARCHAR2 (10 BYTE)
 , v858    VARCHAR2 (10 BYTE)
 , v859    VARCHAR2 (10 BYTE)
 , v860    VARCHAR2 (10 BYTE)
 , v861    VARCHAR2 (10 BYTE)
 , v862    VARCHAR2 (10 BYTE)
 , v863    VARCHAR2 (10 BYTE)
 , v864    VARCHAR2 (10 BYTE)
 , v865    VARCHAR2 (10 BYTE)
 , v866    VARCHAR2 (10 BYTE)
 , v867    VARCHAR2 (10 BYTE)
 , v868    VARCHAR2 (10 BYTE)
 , v869    VARCHAR2 (10 BYTE)
 , v870    VARCHAR2 (10 BYTE)
 , v871    VARCHAR2 (10 BYTE)
 , v872    VARCHAR2 (10 BYTE)
 , v873    VARCHAR2 (10 BYTE)
 , v874    VARCHAR2 (10 BYTE)
 , v875    VARCHAR2 (10 BYTE)
 , v876    VARCHAR2 (10 BYTE)
 , v877    VARCHAR2 (10 BYTE)
 , v878    VARCHAR2 (10 BYTE)
 , v879    VARCHAR2 (10 BYTE)
 , v880    VARCHAR2 (10 BYTE)
 , v881    VARCHAR2 (10 BYTE)
 , v882    VARCHAR2 (10 BYTE)
 , v883    VARCHAR2 (10 BYTE)
 , v884    VARCHAR2 (10 BYTE)
 , v885    VARCHAR2 (10 BYTE)
 , v886    VARCHAR2 (10 BYTE)
 , v887    VARCHAR2 (10 BYTE)
 , v888    VARCHAR2 (10 BYTE)
 , v889    VARCHAR2 (10 BYTE)
 , v890    VARCHAR2 (10 BYTE)
 , v891    VARCHAR2 (10 BYTE)
 , v892    VARCHAR2 (10 BYTE)
 , v893    VARCHAR2 (10 BYTE)
 , v894    VARCHAR2 (10 BYTE)
 , v895    VARCHAR2 (10 BYTE)
 , v896    VARCHAR2 (10 BYTE)
 , v897    VARCHAR2 (10 BYTE)
 , v898    VARCHAR2 (10 BYTE)
 , v899    VARCHAR2 (10 BYTE)
 , v900    VARCHAR2 (10 BYTE)
 , v901    VARCHAR2 (10 BYTE)
 , v902    VARCHAR2 (10 BYTE)
 , v903    VARCHAR2 (10 BYTE)
 , v904    VARCHAR2 (10 BYTE)
 , v905    VARCHAR2 (10 BYTE)
 , v906    VARCHAR2 (10 BYTE)
 , v907    VARCHAR2 (10 BYTE)
 , v908    VARCHAR2 (10 BYTE)
 , v909    VARCHAR2 (10 BYTE)
 , v910    VARCHAR2 (10 BYTE)
 , v911    VARCHAR2 (10 BYTE)
 , v912    VARCHAR2 (10 BYTE)
 , v913    VARCHAR2 (10 BYTE)
 , v914    VARCHAR2 (10 BYTE)
 , v915    VARCHAR2 (10 BYTE)
 , v916    VARCHAR2 (10 BYTE)
 , v917    VARCHAR2 (10 BYTE)
 , v918    VARCHAR2 (10 BYTE)
 , v919    VARCHAR2 (10 BYTE)
 , v920    VARCHAR2 (10 BYTE)
 , v921    VARCHAR2 (10 BYTE)
 , v922    VARCHAR2 (10 BYTE)
 , v923    VARCHAR2 (10 BYTE)
 , v924    VARCHAR2 (10 BYTE)
 , v925    VARCHAR2 (10 BYTE)
 , v926    VARCHAR2 (10 BYTE)
 , v927    VARCHAR2 (10 BYTE)
 , v928    VARCHAR2 (10 BYTE)
 , v929    VARCHAR2 (10 BYTE)
 , v930    VARCHAR2 (10 BYTE)
 , v931    VARCHAR2 (10 BYTE)
 , v932    VARCHAR2 (10 BYTE)
 , v933    VARCHAR2 (10 BYTE)
 , v934    VARCHAR2 (10 BYTE)
 , v935    VARCHAR2 (10 BYTE)
 , v936    VARCHAR2 (10 BYTE)
 , v937    VARCHAR2 (10 BYTE)
 , v938    VARCHAR2 (10 BYTE)
 , v939    VARCHAR2 (10 BYTE)
 , v940    VARCHAR2 (10 BYTE)
 , v941    VARCHAR2 (10 BYTE)
 , v942    VARCHAR2 (10 BYTE)
 , v943    VARCHAR2 (10 BYTE)
 , v944    VARCHAR2 (10 BYTE)
 , v945    VARCHAR2 (10 BYTE)
 , v946    VARCHAR2 (10 BYTE)
 , v947    VARCHAR2 (10 BYTE)
 , v948    VARCHAR2 (10 BYTE)
 , v949    VARCHAR2 (10 BYTE)
 , v950    VARCHAR2 (10 BYTE)
 , v951    VARCHAR2 (10 BYTE)
 , v952    VARCHAR2 (10 BYTE)
 , v953    VARCHAR2 (10 BYTE)
 , v954    VARCHAR2 (10 BYTE)
 , v955    VARCHAR2 (10 BYTE)
 , v956    VARCHAR2 (10 BYTE)
 , v957    VARCHAR2 (10 BYTE)
 , v958    VARCHAR2 (10 BYTE)
 , v959    VARCHAR2 (10 BYTE)
 , v960    VARCHAR2 (10 BYTE)
 , v961    VARCHAR2 (10 BYTE)
 , v962    VARCHAR2 (10 BYTE)
 , v963    VARCHAR2 (10 BYTE)
 , v964    VARCHAR2 (10 BYTE)
 , v965    VARCHAR2 (10 BYTE)
 , v966    VARCHAR2 (10 BYTE)
 , v967    VARCHAR2 (10 BYTE)
 , v968    VARCHAR2 (10 BYTE)
 , v969    VARCHAR2 (10 BYTE)
 , v970    VARCHAR2 (10 BYTE)
 , v971    VARCHAR2 (10 BYTE)
 , v972    VARCHAR2 (10 BYTE)
 , v973    VARCHAR2 (10 BYTE)
 , v974    VARCHAR2 (10 BYTE)
 , v975    VARCHAR2 (10 BYTE)
 , v976    VARCHAR2 (10 BYTE)
 , v977    VARCHAR2 (10 BYTE)
 , v978    VARCHAR2 (10 BYTE)
 , v979    VARCHAR2 (10 BYTE)
 , v980    VARCHAR2 (10 BYTE)
 , v981    VARCHAR2 (10 BYTE)
 , v982    VARCHAR2 (10 BYTE)
 , v983    VARCHAR2 (10 BYTE)
 , v984    VARCHAR2 (10 BYTE)
 , v985    VARCHAR2 (10 BYTE)
 , v986    VARCHAR2 (10 BYTE)
 , v987    VARCHAR2 (10 BYTE)
 , v988    VARCHAR2 (10 BYTE)
 , v989    VARCHAR2 (10 BYTE)
 , v990    VARCHAR2 (10 BYTE)
 , v991    VARCHAR2 (10 BYTE)
 , v992    VARCHAR2 (10 BYTE)
 , v993    VARCHAR2 (10 BYTE)
 , v994    VARCHAR2 (10 BYTE)
 , v995    VARCHAR2 (10 BYTE)
 , v996    VARCHAR2 (10 BYTE)
 , v997    VARCHAR2 (10 BYTE)
 , v998    VARCHAR2 (10 BYTE)
 , v999    VARCHAR2 (10 BYTE)
 , v1000   VARCHAR2 (10 BYTE)
);

DROP TRIGGER v$trgupdt_97950;

CREATE OR REPLACE TRIGGER v$trgupdt_97950
   AFTER UPDATE
   ON mytab
   FOR EACH ROW
DECLARE
   strsql     CLOB;
   colnames   CLOB := 'errcolname_type(';
   i          NUMBER := 0;
   oldval     VARCHAR2 (4000);
   newval     VARCHAR2 (4000);

   PROCEDURE checkforfirsttime
   IS
   BEGIN
      IF i = 0
      THEN
         i := 1;
      ELSE
         colnames := colnames || ',';
      END IF;
   END;
BEGIN
   IF UPDATING
   THEN
      strsql :=
         'INSERT INTO tab1_2_log VALUES(''UPDATED'',''MYTAB'',substr('''
         || :new.ROWID
         || ''',1),';

      FOR r
         IN (  SELECT column_name
                 FROM all_tab_columns
                WHERE table_name = 'MYTAB' AND owner = USER
                      AND data_type IN
                             ('CHAR'
                            , 'DATE'
                            , 'DECIMAL'
                            , 'FLOAT'
                            , 'INTEGER'
                            , 'NUMBER'
                            , 'NUMERIC'
                            , 'RAW'
                            , 'REAL'
                            , 'SMALLINT'
                            , 'VARCHAR2'
                            , 'NVARCHAR2'
                            , 'NCHAR')
             ORDER BY column_id)
      LOOP
         oldval := NULL;
         newval := NULL;

         IF UPDATING ('V1') AND 'V1' = r.column_name
         THEN
            oldval := :old.v1;
            newval := :new.v1;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V2') AND 'V2' = r.column_name
         THEN
            oldval := :old.v2;
            newval := :new.v2;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V3') AND 'V3' = r.column_name
         THEN
            oldval := :old.v3;
            newval := :new.v3;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V4') AND 'V4' = r.column_name
         THEN
            oldval := :old.v4;
            newval := :new.v4;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V5') AND 'V5' = r.column_name
         THEN
            oldval := :old.v5;
            newval := :new.v5;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V6') AND 'V6' = r.column_name
         THEN
            oldval := :old.v6;
            newval := :new.v6;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V7') AND 'V7' = r.column_name
         THEN
            oldval := :old.v7;
            newval := :new.v7;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V8') AND 'V8' = r.column_name
         THEN
            oldval := :old.v8;
            newval := :new.v8;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V9') AND 'V9' = r.column_name
         THEN
            oldval := :old.v9;
            newval := :new.v9;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V10') AND 'V10' = r.column_name
         THEN
            oldval := :old.v10;
            newval := :new.v10;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V11') AND 'V11' = r.column_name
         THEN
            oldval := :old.v11;
            newval := :new.v11;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V12') AND 'V12' = r.column_name
         THEN
            oldval := :old.v12;
            newval := :new.v12;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V13') AND 'V13' = r.column_name
         THEN
            oldval := :old.v13;
            newval := :new.v13;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V14') AND 'V14' = r.column_name
         THEN
            oldval := :old.v14;
            newval := :new.v14;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V15') AND 'V15' = r.column_name
         THEN
            oldval := :old.v15;
            newval := :new.v15;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V16') AND 'V16' = r.column_name
         THEN
            oldval := :old.v16;
            newval := :new.v16;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V17') AND 'V17' = r.column_name
         THEN
            oldval := :old.v17;
            newval := :new.v17;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V18') AND 'V18' = r.column_name
         THEN
            oldval := :old.v18;
            newval := :new.v18;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V19') AND 'V19' = r.column_name
         THEN
            oldval := :old.v19;
            newval := :new.v19;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V20') AND 'V20' = r.column_name
         THEN
            oldval := :old.v20;
            newval := :new.v20;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V21') AND 'V21' = r.column_name
         THEN
            oldval := :old.v21;
            newval := :new.v21;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V22') AND 'V22' = r.column_name
         THEN
            oldval := :old.v22;
            newval := :new.v22;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V23') AND 'V23' = r.column_name
         THEN
            oldval := :old.v23;
            newval := :new.v23;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V24') AND 'V24' = r.column_name
         THEN
            oldval := :old.v24;
            newval := :new.v24;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V25') AND 'V25' = r.column_name
         THEN
            oldval := :old.v25;
            newval := :new.v25;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V26') AND 'V26' = r.column_name
         THEN
            oldval := :old.v26;
            newval := :new.v26;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V27') AND 'V27' = r.column_name
         THEN
            oldval := :old.v27;
            newval := :new.v27;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V28') AND 'V28' = r.column_name
         THEN
            oldval := :old.v28;
            newval := :new.v28;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V29') AND 'V29' = r.column_name
         THEN
            oldval := :old.v29;
            newval := :new.v29;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V30') AND 'V30' = r.column_name
         THEN
            oldval := :old.v30;
            newval := :new.v30;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V31') AND 'V31' = r.column_name
         THEN
            oldval := :old.v31;
            newval := :new.v31;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V32') AND 'V32' = r.column_name
         THEN
            oldval := :old.v32;
            newval := :new.v32;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V33') AND 'V33' = r.column_name
         THEN
            oldval := :old.v33;
            newval := :new.v33;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V34') AND 'V34' = r.column_name
         THEN
            oldval := :old.v34;
            newval := :new.v34;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V35') AND 'V35' = r.column_name
         THEN
            oldval := :old.v35;
            newval := :new.v35;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V36') AND 'V36' = r.column_name
         THEN
            oldval := :old.v36;
            newval := :new.v36;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V37') AND 'V37' = r.column_name
         THEN
            oldval := :old.v37;
            newval := :new.v37;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V38') AND 'V38' = r.column_name
         THEN
            oldval := :old.v38;
            newval := :new.v38;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V39') AND 'V39' = r.column_name
         THEN
            oldval := :old.v39;
            newval := :new.v39;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V40') AND 'V40' = r.column_name
         THEN
            oldval := :old.v40;
            newval := :new.v40;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V41') AND 'V41' = r.column_name
         THEN
            oldval := :old.v41;
            newval := :new.v41;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V42') AND 'V42' = r.column_name
         THEN
            oldval := :old.v42;
            newval := :new.v42;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V43') AND 'V43' = r.column_name
         THEN
            oldval := :old.v43;
            newval := :new.v43;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V44') AND 'V44' = r.column_name
         THEN
            oldval := :old.v44;
            newval := :new.v44;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V45') AND 'V45' = r.column_name
         THEN
            oldval := :old.v45;
            newval := :new.v45;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V46') AND 'V46' = r.column_name
         THEN
            oldval := :old.v46;
            newval := :new.v46;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V47') AND 'V47' = r.column_name
         THEN
            oldval := :old.v47;
            newval := :new.v47;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V48') AND 'V48' = r.column_name
         THEN
            oldval := :old.v48;
            newval := :new.v48;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V49') AND 'V49' = r.column_name
         THEN
            oldval := :old.v49;
            newval := :new.v49;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V50') AND 'V50' = r.column_name
         THEN
            oldval := :old.v50;
            newval := :new.v50;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V51') AND 'V51' = r.column_name
         THEN
            oldval := :old.v51;
            newval := :new.v51;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V52') AND 'V52' = r.column_name
         THEN
            oldval := :old.v52;
            newval := :new.v52;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V53') AND 'V53' = r.column_name
         THEN
            oldval := :old.v53;
            newval := :new.v53;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V54') AND 'V54' = r.column_name
         THEN
            oldval := :old.v54;
            newval := :new.v54;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V55') AND 'V55' = r.column_name
         THEN
            oldval := :old.v55;
            newval := :new.v55;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V56') AND 'V56' = r.column_name
         THEN
            oldval := :old.v56;
            newval := :new.v56;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V57') AND 'V57' = r.column_name
         THEN
            oldval := :old.v57;
            newval := :new.v57;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V58') AND 'V58' = r.column_name
         THEN
            oldval := :old.v58;
            newval := :new.v58;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V59') AND 'V59' = r.column_name
         THEN
            oldval := :old.v59;
            newval := :new.v59;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V60') AND 'V60' = r.column_name
         THEN
            oldval := :old.v60;
            newval := :new.v60;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V61') AND 'V61' = r.column_name
         THEN
            oldval := :old.v61;
            newval := :new.v61;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V62') AND 'V62' = r.column_name
         THEN
            oldval := :old.v62;
            newval := :new.v62;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V63') AND 'V63' = r.column_name
         THEN
            oldval := :old.v63;
            newval := :new.v63;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V64') AND 'V64' = r.column_name
         THEN
            oldval := :old.v64;
            newval := :new.v64;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V65') AND 'V65' = r.column_name
         THEN
            oldval := :old.v65;
            newval := :new.v65;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V66') AND 'V66' = r.column_name
         THEN
            oldval := :old.v66;
            newval := :new.v66;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V67') AND 'V67' = r.column_name
         THEN
            oldval := :old.v67;
            newval := :new.v67;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V68') AND 'V68' = r.column_name
         THEN
            oldval := :old.v68;
            newval := :new.v68;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V69') AND 'V69' = r.column_name
         THEN
            oldval := :old.v69;
            newval := :new.v69;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V70') AND 'V70' = r.column_name
         THEN
            oldval := :old.v70;
            newval := :new.v70;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V71') AND 'V71' = r.column_name
         THEN
            oldval := :old.v71;
            newval := :new.v71;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V72') AND 'V72' = r.column_name
         THEN
            oldval := :old.v72;
            newval := :new.v72;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V73') AND 'V73' = r.column_name
         THEN
            oldval := :old.v73;
            newval := :new.v73;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V74') AND 'V74' = r.column_name
         THEN
            oldval := :old.v74;
            newval := :new.v74;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V75') AND 'V75' = r.column_name
         THEN
            oldval := :old.v75;
            newval := :new.v75;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V76') AND 'V76' = r.column_name
         THEN
            oldval := :old.v76;
            newval := :new.v76;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V77') AND 'V77' = r.column_name
         THEN
            oldval := :old.v77;
            newval := :new.v77;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V78') AND 'V78' = r.column_name
         THEN
            oldval := :old.v78;
            newval := :new.v78;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V79') AND 'V79' = r.column_name
         THEN
            oldval := :old.v79;
            newval := :new.v79;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V80') AND 'V80' = r.column_name
         THEN
            oldval := :old.v80;
            newval := :new.v80;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V81') AND 'V81' = r.column_name
         THEN
            oldval := :old.v81;
            newval := :new.v81;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V82') AND 'V82' = r.column_name
         THEN
            oldval := :old.v82;
            newval := :new.v82;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V83') AND 'V83' = r.column_name
         THEN
            oldval := :old.v83;
            newval := :new.v83;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V84') AND 'V84' = r.column_name
         THEN
            oldval := :old.v84;
            newval := :new.v84;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V85') AND 'V85' = r.column_name
         THEN
            oldval := :old.v85;
            newval := :new.v85;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V86') AND 'V86' = r.column_name
         THEN
            oldval := :old.v86;
            newval := :new.v86;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V87') AND 'V87' = r.column_name
         THEN
            oldval := :old.v87;
            newval := :new.v87;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V88') AND 'V88' = r.column_name
         THEN
            oldval := :old.v88;
            newval := :new.v88;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V89') AND 'V89' = r.column_name
         THEN
            oldval := :old.v89;
            newval := :new.v89;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V90') AND 'V90' = r.column_name
         THEN
            oldval := :old.v90;
            newval := :new.v90;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V91') AND 'V91' = r.column_name
         THEN
            oldval := :old.v91;
            newval := :new.v91;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V92') AND 'V92' = r.column_name
         THEN
            oldval := :old.v92;
            newval := :new.v92;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V93') AND 'V93' = r.column_name
         THEN
            oldval := :old.v93;
            newval := :new.v93;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V94') AND 'V94' = r.column_name
         THEN
            oldval := :old.v94;
            newval := :new.v94;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V95') AND 'V95' = r.column_name
         THEN
            oldval := :old.v95;
            newval := :new.v95;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V96') AND 'V96' = r.column_name
         THEN
            oldval := :old.v96;
            newval := :new.v96;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V97') AND 'V97' = r.column_name
         THEN
            oldval := :old.v97;
            newval := :new.v97;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V98') AND 'V98' = r.column_name
         THEN
            oldval := :old.v98;
            newval := :new.v98;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V99') AND 'V99' = r.column_name
         THEN
            oldval := :old.v99;
            newval := :new.v99;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V100') AND 'V100' = r.column_name
         THEN
            oldval := :old.v100;
            newval := :new.v100;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V101') AND 'V101' = r.column_name
         THEN
            oldval := :old.v101;
            newval := :new.v101;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V102') AND 'V102' = r.column_name
         THEN
            oldval := :old.v102;
            newval := :new.v102;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V103') AND 'V103' = r.column_name
         THEN
            oldval := :old.v103;
            newval := :new.v103;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V104') AND 'V104' = r.column_name
         THEN
            oldval := :old.v104;
            newval := :new.v104;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V105') AND 'V105' = r.column_name
         THEN
            oldval := :old.v105;
            newval := :new.v105;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V106') AND 'V106' = r.column_name
         THEN
            oldval := :old.v106;
            newval := :new.v106;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V107') AND 'V107' = r.column_name
         THEN
            oldval := :old.v107;
            newval := :new.v107;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V108') AND 'V108' = r.column_name
         THEN
            oldval := :old.v108;
            newval := :new.v108;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V109') AND 'V109' = r.column_name
         THEN
            oldval := :old.v109;
            newval := :new.v109;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V110') AND 'V110' = r.column_name
         THEN
            oldval := :old.v110;
            newval := :new.v110;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V111') AND 'V111' = r.column_name
         THEN
            oldval := :old.v111;
            newval := :new.v111;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V112') AND 'V112' = r.column_name
         THEN
            oldval := :old.v112;
            newval := :new.v112;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V113') AND 'V113' = r.column_name
         THEN
            oldval := :old.v113;
            newval := :new.v113;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V114') AND 'V114' = r.column_name
         THEN
            oldval := :old.v114;
            newval := :new.v114;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V115') AND 'V115' = r.column_name
         THEN
            oldval := :old.v115;
            newval := :new.v115;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V116') AND 'V116' = r.column_name
         THEN
            oldval := :old.v116;
            newval := :new.v116;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V117') AND 'V117' = r.column_name
         THEN
            oldval := :old.v117;
            newval := :new.v117;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V118') AND 'V118' = r.column_name
         THEN
            oldval := :old.v118;
            newval := :new.v118;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V119') AND 'V119' = r.column_name
         THEN
            oldval := :old.v119;
            newval := :new.v119;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V120') AND 'V120' = r.column_name
         THEN
            oldval := :old.v120;
            newval := :new.v120;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V121') AND 'V121' = r.column_name
         THEN
            oldval := :old.v121;
            newval := :new.v121;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V122') AND 'V122' = r.column_name
         THEN
            oldval := :old.v122;
            newval := :new.v122;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V123') AND 'V123' = r.column_name
         THEN
            oldval := :old.v123;
            newval := :new.v123;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V124') AND 'V124' = r.column_name
         THEN
            oldval := :old.v124;
            newval := :new.v124;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V125') AND 'V125' = r.column_name
         THEN
            oldval := :old.v125;
            newval := :new.v125;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V126') AND 'V126' = r.column_name
         THEN
            oldval := :old.v126;
            newval := :new.v126;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V127') AND 'V127' = r.column_name
         THEN
            oldval := :old.v127;
            newval := :new.v127;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V128') AND 'V128' = r.column_name
         THEN
            oldval := :old.v128;
            newval := :new.v128;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V129') AND 'V129' = r.column_name
         THEN
            oldval := :old.v129;
            newval := :new.v129;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V130') AND 'V130' = r.column_name
         THEN
            oldval := :old.v130;
            newval := :new.v130;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V131') AND 'V131' = r.column_name
         THEN
            oldval := :old.v131;
            newval := :new.v131;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V132') AND 'V132' = r.column_name
         THEN
            oldval := :old.v132;
            newval := :new.v132;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V133') AND 'V133' = r.column_name
         THEN
            oldval := :old.v133;
            newval := :new.v133;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V134') AND 'V134' = r.column_name
         THEN
            oldval := :old.v134;
            newval := :new.v134;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V135') AND 'V135' = r.column_name
         THEN
            oldval := :old.v135;
            newval := :new.v135;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V136') AND 'V136' = r.column_name
         THEN
            oldval := :old.v136;
            newval := :new.v136;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V137') AND 'V137' = r.column_name
         THEN
            oldval := :old.v137;
            newval := :new.v137;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V138') AND 'V138' = r.column_name
         THEN
            oldval := :old.v138;
            newval := :new.v138;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V139') AND 'V139' = r.column_name
         THEN
            oldval := :old.v139;
            newval := :new.v139;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V140') AND 'V140' = r.column_name
         THEN
            oldval := :old.v140;
            newval := :new.v140;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V141') AND 'V141' = r.column_name
         THEN
            oldval := :old.v141;
            newval := :new.v141;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V142') AND 'V142' = r.column_name
         THEN
            oldval := :old.v142;
            newval := :new.v142;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V143') AND 'V143' = r.column_name
         THEN
            oldval := :old.v143;
            newval := :new.v143;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V144') AND 'V144' = r.column_name
         THEN
            oldval := :old.v144;
            newval := :new.v144;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V145') AND 'V145' = r.column_name
         THEN
            oldval := :old.v145;
            newval := :new.v145;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V146') AND 'V146' = r.column_name
         THEN
            oldval := :old.v146;
            newval := :new.v146;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V147') AND 'V147' = r.column_name
         THEN
            oldval := :old.v147;
            newval := :new.v147;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V148') AND 'V148' = r.column_name
         THEN
            oldval := :old.v148;
            newval := :new.v148;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V149') AND 'V149' = r.column_name
         THEN
            oldval := :old.v149;
            newval := :new.v149;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V150') AND 'V150' = r.column_name
         THEN
            oldval := :old.v150;
            newval := :new.v150;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V151') AND 'V151' = r.column_name
         THEN
            oldval := :old.v151;
            newval := :new.v151;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V152') AND 'V152' = r.column_name
         THEN
            oldval := :old.v152;
            newval := :new.v152;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V153') AND 'V153' = r.column_name
         THEN
            oldval := :old.v153;
            newval := :new.v153;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V154') AND 'V154' = r.column_name
         THEN
            oldval := :old.v154;
            newval := :new.v154;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V155') AND 'V155' = r.column_name
         THEN
            oldval := :old.v155;
            newval := :new.v155;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V156') AND 'V156' = r.column_name
         THEN
            oldval := :old.v156;
            newval := :new.v156;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V157') AND 'V157' = r.column_name
         THEN
            oldval := :old.v157;
            newval := :new.v157;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V158') AND 'V158' = r.column_name
         THEN
            oldval := :old.v158;
            newval := :new.v158;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V159') AND 'V159' = r.column_name
         THEN
            oldval := :old.v159;
            newval := :new.v159;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V160') AND 'V160' = r.column_name
         THEN
            oldval := :old.v160;
            newval := :new.v160;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V161') AND 'V161' = r.column_name
         THEN
            oldval := :old.v161;
            newval := :new.v161;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V162') AND 'V162' = r.column_name
         THEN
            oldval := :old.v162;
            newval := :new.v162;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V163') AND 'V163' = r.column_name
         THEN
            oldval := :old.v163;
            newval := :new.v163;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V164') AND 'V164' = r.column_name
         THEN
            oldval := :old.v164;
            newval := :new.v164;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V165') AND 'V165' = r.column_name
         THEN
            oldval := :old.v165;
            newval := :new.v165;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V166') AND 'V166' = r.column_name
         THEN
            oldval := :old.v166;
            newval := :new.v166;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V167') AND 'V167' = r.column_name
         THEN
            oldval := :old.v167;
            newval := :new.v167;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V168') AND 'V168' = r.column_name
         THEN
            oldval := :old.v168;
            newval := :new.v168;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V169') AND 'V169' = r.column_name
         THEN
            oldval := :old.v169;
            newval := :new.v169;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V170') AND 'V170' = r.column_name
         THEN
            oldval := :old.v170;
            newval := :new.v170;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V171') AND 'V171' = r.column_name
         THEN
            oldval := :old.v171;
            newval := :new.v171;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V172') AND 'V172' = r.column_name
         THEN
            oldval := :old.v172;
            newval := :new.v172;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V173') AND 'V173' = r.column_name
         THEN
            oldval := :old.v173;
            newval := :new.v173;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V174') AND 'V174' = r.column_name
         THEN
            oldval := :old.v174;
            newval := :new.v174;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V175') AND 'V175' = r.column_name
         THEN
            oldval := :old.v175;
            newval := :new.v175;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V176') AND 'V176' = r.column_name
         THEN
            oldval := :old.v176;
            newval := :new.v176;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V177') AND 'V177' = r.column_name
         THEN
            oldval := :old.v177;
            newval := :new.v177;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V178') AND 'V178' = r.column_name
         THEN
            oldval := :old.v178;
            newval := :new.v178;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V179') AND 'V179' = r.column_name
         THEN
            oldval := :old.v179;
            newval := :new.v179;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V180') AND 'V180' = r.column_name
         THEN
            oldval := :old.v180;
            newval := :new.v180;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V181') AND 'V181' = r.column_name
         THEN
            oldval := :old.v181;
            newval := :new.v181;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V182') AND 'V182' = r.column_name
         THEN
            oldval := :old.v182;
            newval := :new.v182;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V183') AND 'V183' = r.column_name
         THEN
            oldval := :old.v183;
            newval := :new.v183;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V184') AND 'V184' = r.column_name
         THEN
            oldval := :old.v184;
            newval := :new.v184;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V185') AND 'V185' = r.column_name
         THEN
            oldval := :old.v185;
            newval := :new.v185;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V186') AND 'V186' = r.column_name
         THEN
            oldval := :old.v186;
            newval := :new.v186;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V187') AND 'V187' = r.column_name
         THEN
            oldval := :old.v187;
            newval := :new.v187;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V188') AND 'V188' = r.column_name
         THEN
            oldval := :old.v188;
            newval := :new.v188;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V189') AND 'V189' = r.column_name
         THEN
            oldval := :old.v189;
            newval := :new.v189;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V190') AND 'V190' = r.column_name
         THEN
            oldval := :old.v190;
            newval := :new.v190;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V191') AND 'V191' = r.column_name
         THEN
            oldval := :old.v191;
            newval := :new.v191;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V192') AND 'V192' = r.column_name
         THEN
            oldval := :old.v192;
            newval := :new.v192;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V193') AND 'V193' = r.column_name
         THEN
            oldval := :old.v193;
            newval := :new.v193;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V194') AND 'V194' = r.column_name
         THEN
            oldval := :old.v194;
            newval := :new.v194;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V195') AND 'V195' = r.column_name
         THEN
            oldval := :old.v195;
            newval := :new.v195;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V196') AND 'V196' = r.column_name
         THEN
            oldval := :old.v196;
            newval := :new.v196;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V197') AND 'V197' = r.column_name
         THEN
            oldval := :old.v197;
            newval := :new.v197;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V198') AND 'V198' = r.column_name
         THEN
            oldval := :old.v198;
            newval := :new.v198;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V199') AND 'V199' = r.column_name
         THEN
            oldval := :old.v199;
            newval := :new.v199;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V200') AND 'V200' = r.column_name
         THEN
            oldval := :old.v200;
            newval := :new.v200;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V201') AND 'V201' = r.column_name
         THEN
            oldval := :old.v201;
            newval := :new.v201;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V202') AND 'V202' = r.column_name
         THEN
            oldval := :old.v202;
            newval := :new.v202;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V203') AND 'V203' = r.column_name
         THEN
            oldval := :old.v203;
            newval := :new.v203;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V204') AND 'V204' = r.column_name
         THEN
            oldval := :old.v204;
            newval := :new.v204;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V205') AND 'V205' = r.column_name
         THEN
            oldval := :old.v205;
            newval := :new.v205;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V206') AND 'V206' = r.column_name
         THEN
            oldval := :old.v206;
            newval := :new.v206;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V207') AND 'V207' = r.column_name
         THEN
            oldval := :old.v207;
            newval := :new.v207;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V208') AND 'V208' = r.column_name
         THEN
            oldval := :old.v208;
            newval := :new.v208;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V209') AND 'V209' = r.column_name
         THEN
            oldval := :old.v209;
            newval := :new.v209;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V210') AND 'V210' = r.column_name
         THEN
            oldval := :old.v210;
            newval := :new.v210;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V211') AND 'V211' = r.column_name
         THEN
            oldval := :old.v211;
            newval := :new.v211;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V212') AND 'V212' = r.column_name
         THEN
            oldval := :old.v212;
            newval := :new.v212;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V213') AND 'V213' = r.column_name
         THEN
            oldval := :old.v213;
            newval := :new.v213;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V214') AND 'V214' = r.column_name
         THEN
            oldval := :old.v214;
            newval := :new.v214;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V215') AND 'V215' = r.column_name
         THEN
            oldval := :old.v215;
            newval := :new.v215;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V216') AND 'V216' = r.column_name
         THEN
            oldval := :old.v216;
            newval := :new.v216;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V217') AND 'V217' = r.column_name
         THEN
            oldval := :old.v217;
            newval := :new.v217;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V218') AND 'V218' = r.column_name
         THEN
            oldval := :old.v218;
            newval := :new.v218;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V219') AND 'V219' = r.column_name
         THEN
            oldval := :old.v219;
            newval := :new.v219;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V220') AND 'V220' = r.column_name
         THEN
            oldval := :old.v220;
            newval := :new.v220;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V221') AND 'V221' = r.column_name
         THEN
            oldval := :old.v221;
            newval := :new.v221;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V222') AND 'V222' = r.column_name
         THEN
            oldval := :old.v222;
            newval := :new.v222;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V223') AND 'V223' = r.column_name
         THEN
            oldval := :old.v223;
            newval := :new.v223;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V224') AND 'V224' = r.column_name
         THEN
            oldval := :old.v224;
            newval := :new.v224;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V225') AND 'V225' = r.column_name
         THEN
            oldval := :old.v225;
            newval := :new.v225;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V226') AND 'V226' = r.column_name
         THEN
            oldval := :old.v226;
            newval := :new.v226;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V227') AND 'V227' = r.column_name
         THEN
            oldval := :old.v227;
            newval := :new.v227;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V228') AND 'V228' = r.column_name
         THEN
            oldval := :old.v228;
            newval := :new.v228;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V229') AND 'V229' = r.column_name
         THEN
            oldval := :old.v229;
            newval := :new.v229;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V230') AND 'V230' = r.column_name
         THEN
            oldval := :old.v230;
            newval := :new.v230;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V231') AND 'V231' = r.column_name
         THEN
            oldval := :old.v231;
            newval := :new.v231;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V232') AND 'V232' = r.column_name
         THEN
            oldval := :old.v232;
            newval := :new.v232;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V233') AND 'V233' = r.column_name
         THEN
            oldval := :old.v233;
            newval := :new.v233;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V234') AND 'V234' = r.column_name
         THEN
            oldval := :old.v234;
            newval := :new.v234;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V235') AND 'V235' = r.column_name
         THEN
            oldval := :old.v235;
            newval := :new.v235;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V236') AND 'V236' = r.column_name
         THEN
            oldval := :old.v236;
            newval := :new.v236;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V237') AND 'V237' = r.column_name
         THEN
            oldval := :old.v237;
            newval := :new.v237;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V238') AND 'V238' = r.column_name
         THEN
            oldval := :old.v238;
            newval := :new.v238;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V239') AND 'V239' = r.column_name
         THEN
            oldval := :old.v239;
            newval := :new.v239;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V240') AND 'V240' = r.column_name
         THEN
            oldval := :old.v240;
            newval := :new.v240;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V241') AND 'V241' = r.column_name
         THEN
            oldval := :old.v241;
            newval := :new.v241;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V242') AND 'V242' = r.column_name
         THEN
            oldval := :old.v242;
            newval := :new.v242;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V243') AND 'V243' = r.column_name
         THEN
            oldval := :old.v243;
            newval := :new.v243;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V244') AND 'V244' = r.column_name
         THEN
            oldval := :old.v244;
            newval := :new.v244;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V245') AND 'V245' = r.column_name
         THEN
            oldval := :old.v245;
            newval := :new.v245;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V246') AND 'V246' = r.column_name
         THEN
            oldval := :old.v246;
            newval := :new.v246;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V247') AND 'V247' = r.column_name
         THEN
            oldval := :old.v247;
            newval := :new.v247;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V248') AND 'V248' = r.column_name
         THEN
            oldval := :old.v248;
            newval := :new.v248;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V249') AND 'V249' = r.column_name
         THEN
            oldval := :old.v249;
            newval := :new.v249;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V250') AND 'V250' = r.column_name
         THEN
            oldval := :old.v250;
            newval := :new.v250;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V251') AND 'V251' = r.column_name
         THEN
            oldval := :old.v251;
            newval := :new.v251;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V252') AND 'V252' = r.column_name
         THEN
            oldval := :old.v252;
            newval := :new.v252;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V253') AND 'V253' = r.column_name
         THEN
            oldval := :old.v253;
            newval := :new.v253;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V254') AND 'V254' = r.column_name
         THEN
            oldval := :old.v254;
            newval := :new.v254;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V255') AND 'V255' = r.column_name
         THEN
            oldval := :old.v255;
            newval := :new.v255;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V256') AND 'V256' = r.column_name
         THEN
            oldval := :old.v256;
            newval := :new.v256;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V257') AND 'V257' = r.column_name
         THEN
            oldval := :old.v257;
            newval := :new.v257;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V258') AND 'V258' = r.column_name
         THEN
            oldval := :old.v258;
            newval := :new.v258;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V259') AND 'V259' = r.column_name
         THEN
            oldval := :old.v259;
            newval := :new.v259;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V260') AND 'V260' = r.column_name
         THEN
            oldval := :old.v260;
            newval := :new.v260;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V261') AND 'V261' = r.column_name
         THEN
            oldval := :old.v261;
            newval := :new.v261;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V262') AND 'V262' = r.column_name
         THEN
            oldval := :old.v262;
            newval := :new.v262;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V263') AND 'V263' = r.column_name
         THEN
            oldval := :old.v263;
            newval := :new.v263;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V264') AND 'V264' = r.column_name
         THEN
            oldval := :old.v264;
            newval := :new.v264;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V265') AND 'V265' = r.column_name
         THEN
            oldval := :old.v265;
            newval := :new.v265;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V266') AND 'V266' = r.column_name
         THEN
            oldval := :old.v266;
            newval := :new.v266;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V267') AND 'V267' = r.column_name
         THEN
            oldval := :old.v267;
            newval := :new.v267;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V268') AND 'V268' = r.column_name
         THEN
            oldval := :old.v268;
            newval := :new.v268;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V269') AND 'V269' = r.column_name
         THEN
            oldval := :old.v269;
            newval := :new.v269;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V270') AND 'V270' = r.column_name
         THEN
            oldval := :old.v270;
            newval := :new.v270;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V271') AND 'V271' = r.column_name
         THEN
            oldval := :old.v271;
            newval := :new.v271;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V272') AND 'V272' = r.column_name
         THEN
            oldval := :old.v272;
            newval := :new.v272;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V273') AND 'V273' = r.column_name
         THEN
            oldval := :old.v273;
            newval := :new.v273;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V274') AND 'V274' = r.column_name
         THEN
            oldval := :old.v274;
            newval := :new.v274;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V275') AND 'V275' = r.column_name
         THEN
            oldval := :old.v275;
            newval := :new.v275;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V276') AND 'V276' = r.column_name
         THEN
            oldval := :old.v276;
            newval := :new.v276;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V277') AND 'V277' = r.column_name
         THEN
            oldval := :old.v277;
            newval := :new.v277;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V278') AND 'V278' = r.column_name
         THEN
            oldval := :old.v278;
            newval := :new.v278;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V279') AND 'V279' = r.column_name
         THEN
            oldval := :old.v279;
            newval := :new.v279;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V280') AND 'V280' = r.column_name
         THEN
            oldval := :old.v280;
            newval := :new.v280;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V281') AND 'V281' = r.column_name
         THEN
            oldval := :old.v281;
            newval := :new.v281;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V282') AND 'V282' = r.column_name
         THEN
            oldval := :old.v282;
            newval := :new.v282;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V283') AND 'V283' = r.column_name
         THEN
            oldval := :old.v283;
            newval := :new.v283;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V284') AND 'V284' = r.column_name
         THEN
            oldval := :old.v284;
            newval := :new.v284;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V285') AND 'V285' = r.column_name
         THEN
            oldval := :old.v285;
            newval := :new.v285;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V286') AND 'V286' = r.column_name
         THEN
            oldval := :old.v286;
            newval := :new.v286;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V287') AND 'V287' = r.column_name
         THEN
            oldval := :old.v287;
            newval := :new.v287;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V288') AND 'V288' = r.column_name
         THEN
            oldval := :old.v288;
            newval := :new.v288;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V289') AND 'V289' = r.column_name
         THEN
            oldval := :old.v289;
            newval := :new.v289;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V290') AND 'V290' = r.column_name
         THEN
            oldval := :old.v290;
            newval := :new.v290;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V291') AND 'V291' = r.column_name
         THEN
            oldval := :old.v291;
            newval := :new.v291;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V292') AND 'V292' = r.column_name
         THEN
            oldval := :old.v292;
            newval := :new.v292;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V293') AND 'V293' = r.column_name
         THEN
            oldval := :old.v293;
            newval := :new.v293;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V294') AND 'V294' = r.column_name
         THEN
            oldval := :old.v294;
            newval := :new.v294;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V295') AND 'V295' = r.column_name
         THEN
            oldval := :old.v295;
            newval := :new.v295;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V296') AND 'V296' = r.column_name
         THEN
            oldval := :old.v296;
            newval := :new.v296;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V297') AND 'V297' = r.column_name
         THEN
            oldval := :old.v297;
            newval := :new.v297;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V298') AND 'V298' = r.column_name
         THEN
            oldval := :old.v298;
            newval := :new.v298;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V299') AND 'V299' = r.column_name
         THEN
            oldval := :old.v299;
            newval := :new.v299;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V300') AND 'V300' = r.column_name
         THEN
            oldval := :old.v300;
            newval := :new.v300;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V301') AND 'V301' = r.column_name
         THEN
            oldval := :old.v301;
            newval := :new.v301;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V302') AND 'V302' = r.column_name
         THEN
            oldval := :old.v302;
            newval := :new.v302;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V303') AND 'V303' = r.column_name
         THEN
            oldval := :old.v303;
            newval := :new.v303;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V304') AND 'V304' = r.column_name
         THEN
            oldval := :old.v304;
            newval := :new.v304;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V305') AND 'V305' = r.column_name
         THEN
            oldval := :old.v305;
            newval := :new.v305;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V306') AND 'V306' = r.column_name
         THEN
            oldval := :old.v306;
            newval := :new.v306;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V307') AND 'V307' = r.column_name
         THEN
            oldval := :old.v307;
            newval := :new.v307;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V308') AND 'V308' = r.column_name
         THEN
            oldval := :old.v308;
            newval := :new.v308;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V309') AND 'V309' = r.column_name
         THEN
            oldval := :old.v309;
            newval := :new.v309;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V310') AND 'V310' = r.column_name
         THEN
            oldval := :old.v310;
            newval := :new.v310;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V311') AND 'V311' = r.column_name
         THEN
            oldval := :old.v311;
            newval := :new.v311;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V312') AND 'V312' = r.column_name
         THEN
            oldval := :old.v312;
            newval := :new.v312;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V313') AND 'V313' = r.column_name
         THEN
            oldval := :old.v313;
            newval := :new.v313;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V314') AND 'V314' = r.column_name
         THEN
            oldval := :old.v314;
            newval := :new.v314;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V315') AND 'V315' = r.column_name
         THEN
            oldval := :old.v315;
            newval := :new.v315;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V316') AND 'V316' = r.column_name
         THEN
            oldval := :old.v316;
            newval := :new.v316;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V317') AND 'V317' = r.column_name
         THEN
            oldval := :old.v317;
            newval := :new.v317;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V318') AND 'V318' = r.column_name
         THEN
            oldval := :old.v318;
            newval := :new.v318;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V319') AND 'V319' = r.column_name
         THEN
            oldval := :old.v319;
            newval := :new.v319;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V320') AND 'V320' = r.column_name
         THEN
            oldval := :old.v320;
            newval := :new.v320;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V321') AND 'V321' = r.column_name
         THEN
            oldval := :old.v321;
            newval := :new.v321;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V322') AND 'V322' = r.column_name
         THEN
            oldval := :old.v322;
            newval := :new.v322;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V323') AND 'V323' = r.column_name
         THEN
            oldval := :old.v323;
            newval := :new.v323;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V324') AND 'V324' = r.column_name
         THEN
            oldval := :old.v324;
            newval := :new.v324;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V325') AND 'V325' = r.column_name
         THEN
            oldval := :old.v325;
            newval := :new.v325;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V326') AND 'V326' = r.column_name
         THEN
            oldval := :old.v326;
            newval := :new.v326;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V327') AND 'V327' = r.column_name
         THEN
            oldval := :old.v327;
            newval := :new.v327;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V328') AND 'V328' = r.column_name
         THEN
            oldval := :old.v328;
            newval := :new.v328;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V329') AND 'V329' = r.column_name
         THEN
            oldval := :old.v329;
            newval := :new.v329;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V330') AND 'V330' = r.column_name
         THEN
            oldval := :old.v330;
            newval := :new.v330;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V331') AND 'V331' = r.column_name
         THEN
            oldval := :old.v331;
            newval := :new.v331;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V332') AND 'V332' = r.column_name
         THEN
            oldval := :old.v332;
            newval := :new.v332;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V333') AND 'V333' = r.column_name
         THEN
            oldval := :old.v333;
            newval := :new.v333;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V334') AND 'V334' = r.column_name
         THEN
            oldval := :old.v334;
            newval := :new.v334;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V335') AND 'V335' = r.column_name
         THEN
            oldval := :old.v335;
            newval := :new.v335;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V336') AND 'V336' = r.column_name
         THEN
            oldval := :old.v336;
            newval := :new.v336;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V337') AND 'V337' = r.column_name
         THEN
            oldval := :old.v337;
            newval := :new.v337;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V338') AND 'V338' = r.column_name
         THEN
            oldval := :old.v338;
            newval := :new.v338;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V339') AND 'V339' = r.column_name
         THEN
            oldval := :old.v339;
            newval := :new.v339;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V340') AND 'V340' = r.column_name
         THEN
            oldval := :old.v340;
            newval := :new.v340;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V341') AND 'V341' = r.column_name
         THEN
            oldval := :old.v341;
            newval := :new.v341;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V342') AND 'V342' = r.column_name
         THEN
            oldval := :old.v342;
            newval := :new.v342;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V343') AND 'V343' = r.column_name
         THEN
            oldval := :old.v343;
            newval := :new.v343;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V344') AND 'V344' = r.column_name
         THEN
            oldval := :old.v344;
            newval := :new.v344;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V345') AND 'V345' = r.column_name
         THEN
            oldval := :old.v345;
            newval := :new.v345;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V346') AND 'V346' = r.column_name
         THEN
            oldval := :old.v346;
            newval := :new.v346;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V347') AND 'V347' = r.column_name
         THEN
            oldval := :old.v347;
            newval := :new.v347;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V348') AND 'V348' = r.column_name
         THEN
            oldval := :old.v348;
            newval := :new.v348;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V349') AND 'V349' = r.column_name
         THEN
            oldval := :old.v349;
            newval := :new.v349;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V350') AND 'V350' = r.column_name
         THEN
            oldval := :old.v350;
            newval := :new.v350;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V351') AND 'V351' = r.column_name
         THEN
            oldval := :old.v351;
            newval := :new.v351;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V352') AND 'V352' = r.column_name
         THEN
            oldval := :old.v352;
            newval := :new.v352;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V353') AND 'V353' = r.column_name
         THEN
            oldval := :old.v353;
            newval := :new.v353;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V354') AND 'V354' = r.column_name
         THEN
            oldval := :old.v354;
            newval := :new.v354;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V355') AND 'V355' = r.column_name
         THEN
            oldval := :old.v355;
            newval := :new.v355;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V356') AND 'V356' = r.column_name
         THEN
            oldval := :old.v356;
            newval := :new.v356;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V357') AND 'V357' = r.column_name
         THEN
            oldval := :old.v357;
            newval := :new.v357;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V358') AND 'V358' = r.column_name
         THEN
            oldval := :old.v358;
            newval := :new.v358;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V359') AND 'V359' = r.column_name
         THEN
            oldval := :old.v359;
            newval := :new.v359;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V360') AND 'V360' = r.column_name
         THEN
            oldval := :old.v360;
            newval := :new.v360;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V361') AND 'V361' = r.column_name
         THEN
            oldval := :old.v361;
            newval := :new.v361;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V362') AND 'V362' = r.column_name
         THEN
            oldval := :old.v362;
            newval := :new.v362;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V363') AND 'V363' = r.column_name
         THEN
            oldval := :old.v363;
            newval := :new.v363;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V364') AND 'V364' = r.column_name
         THEN
            oldval := :old.v364;
            newval := :new.v364;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V365') AND 'V365' = r.column_name
         THEN
            oldval := :old.v365;
            newval := :new.v365;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V366') AND 'V366' = r.column_name
         THEN
            oldval := :old.v366;
            newval := :new.v366;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V367') AND 'V367' = r.column_name
         THEN
            oldval := :old.v367;
            newval := :new.v367;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V368') AND 'V368' = r.column_name
         THEN
            oldval := :old.v368;
            newval := :new.v368;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V369') AND 'V369' = r.column_name
         THEN
            oldval := :old.v369;
            newval := :new.v369;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V370') AND 'V370' = r.column_name
         THEN
            oldval := :old.v370;
            newval := :new.v370;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V371') AND 'V371' = r.column_name
         THEN
            oldval := :old.v371;
            newval := :new.v371;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V372') AND 'V372' = r.column_name
         THEN
            oldval := :old.v372;
            newval := :new.v372;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V373') AND 'V373' = r.column_name
         THEN
            oldval := :old.v373;
            newval := :new.v373;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V374') AND 'V374' = r.column_name
         THEN
            oldval := :old.v374;
            newval := :new.v374;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V375') AND 'V375' = r.column_name
         THEN
            oldval := :old.v375;
            newval := :new.v375;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V376') AND 'V376' = r.column_name
         THEN
            oldval := :old.v376;
            newval := :new.v376;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V377') AND 'V377' = r.column_name
         THEN
            oldval := :old.v377;
            newval := :new.v377;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V378') AND 'V378' = r.column_name
         THEN
            oldval := :old.v378;
            newval := :new.v378;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V379') AND 'V379' = r.column_name
         THEN
            oldval := :old.v379;
            newval := :new.v379;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V380') AND 'V380' = r.column_name
         THEN
            oldval := :old.v380;
            newval := :new.v380;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V381') AND 'V381' = r.column_name
         THEN
            oldval := :old.v381;
            newval := :new.v381;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V382') AND 'V382' = r.column_name
         THEN
            oldval := :old.v382;
            newval := :new.v382;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V383') AND 'V383' = r.column_name
         THEN
            oldval := :old.v383;
            newval := :new.v383;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V384') AND 'V384' = r.column_name
         THEN
            oldval := :old.v384;
            newval := :new.v384;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V385') AND 'V385' = r.column_name
         THEN
            oldval := :old.v385;
            newval := :new.v385;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V386') AND 'V386' = r.column_name
         THEN
            oldval := :old.v386;
            newval := :new.v386;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V387') AND 'V387' = r.column_name
         THEN
            oldval := :old.v387;
            newval := :new.v387;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V388') AND 'V388' = r.column_name
         THEN
            oldval := :old.v388;
            newval := :new.v388;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V389') AND 'V389' = r.column_name
         THEN
            oldval := :old.v389;
            newval := :new.v389;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V390') AND 'V390' = r.column_name
         THEN
            oldval := :old.v390;
            newval := :new.v390;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V391') AND 'V391' = r.column_name
         THEN
            oldval := :old.v391;
            newval := :new.v391;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V392') AND 'V392' = r.column_name
         THEN
            oldval := :old.v392;
            newval := :new.v392;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V393') AND 'V393' = r.column_name
         THEN
            oldval := :old.v393;
            newval := :new.v393;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V394') AND 'V394' = r.column_name
         THEN
            oldval := :old.v394;
            newval := :new.v394;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V395') AND 'V395' = r.column_name
         THEN
            oldval := :old.v395;
            newval := :new.v395;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V396') AND 'V396' = r.column_name
         THEN
            oldval := :old.v396;
            newval := :new.v396;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V397') AND 'V397' = r.column_name
         THEN
            oldval := :old.v397;
            newval := :new.v397;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V398') AND 'V398' = r.column_name
         THEN
            oldval := :old.v398;
            newval := :new.v398;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V399') AND 'V399' = r.column_name
         THEN
            oldval := :old.v399;
            newval := :new.v399;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V400') AND 'V400' = r.column_name
         THEN
            oldval := :old.v400;
            newval := :new.v400;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V401') AND 'V401' = r.column_name
         THEN
            oldval := :old.v401;
            newval := :new.v401;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V402') AND 'V402' = r.column_name
         THEN
            oldval := :old.v402;
            newval := :new.v402;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V403') AND 'V403' = r.column_name
         THEN
            oldval := :old.v403;
            newval := :new.v403;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V404') AND 'V404' = r.column_name
         THEN
            oldval := :old.v404;
            newval := :new.v404;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V405') AND 'V405' = r.column_name
         THEN
            oldval := :old.v405;
            newval := :new.v405;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V406') AND 'V406' = r.column_name
         THEN
            oldval := :old.v406;
            newval := :new.v406;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V407') AND 'V407' = r.column_name
         THEN
            oldval := :old.v407;
            newval := :new.v407;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V408') AND 'V408' = r.column_name
         THEN
            oldval := :old.v408;
            newval := :new.v408;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V409') AND 'V409' = r.column_name
         THEN
            oldval := :old.v409;
            newval := :new.v409;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V410') AND 'V410' = r.column_name
         THEN
            oldval := :old.v410;
            newval := :new.v410;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V411') AND 'V411' = r.column_name
         THEN
            oldval := :old.v411;
            newval := :new.v411;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V412') AND 'V412' = r.column_name
         THEN
            oldval := :old.v412;
            newval := :new.v412;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V413') AND 'V413' = r.column_name
         THEN
            oldval := :old.v413;
            newval := :new.v413;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V414') AND 'V414' = r.column_name
         THEN
            oldval := :old.v414;
            newval := :new.v414;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V415') AND 'V415' = r.column_name
         THEN
            oldval := :old.v415;
            newval := :new.v415;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V416') AND 'V416' = r.column_name
         THEN
            oldval := :old.v416;
            newval := :new.v416;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V417') AND 'V417' = r.column_name
         THEN
            oldval := :old.v417;
            newval := :new.v417;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V418') AND 'V418' = r.column_name
         THEN
            oldval := :old.v418;
            newval := :new.v418;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V419') AND 'V419' = r.column_name
         THEN
            oldval := :old.v419;
            newval := :new.v419;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V420') AND 'V420' = r.column_name
         THEN
            oldval := :old.v420;
            newval := :new.v420;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V421') AND 'V421' = r.column_name
         THEN
            oldval := :old.v421;
            newval := :new.v421;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V422') AND 'V422' = r.column_name
         THEN
            oldval := :old.v422;
            newval := :new.v422;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V423') AND 'V423' = r.column_name
         THEN
            oldval := :old.v423;
            newval := :new.v423;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V424') AND 'V424' = r.column_name
         THEN
            oldval := :old.v424;
            newval := :new.v424;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V425') AND 'V425' = r.column_name
         THEN
            oldval := :old.v425;
            newval := :new.v425;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V426') AND 'V426' = r.column_name
         THEN
            oldval := :old.v426;
            newval := :new.v426;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V427') AND 'V427' = r.column_name
         THEN
            oldval := :old.v427;
            newval := :new.v427;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V428') AND 'V428' = r.column_name
         THEN
            oldval := :old.v428;
            newval := :new.v428;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V429') AND 'V429' = r.column_name
         THEN
            oldval := :old.v429;
            newval := :new.v429;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V430') AND 'V430' = r.column_name
         THEN
            oldval := :old.v430;
            newval := :new.v430;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V431') AND 'V431' = r.column_name
         THEN
            oldval := :old.v431;
            newval := :new.v431;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V432') AND 'V432' = r.column_name
         THEN
            oldval := :old.v432;
            newval := :new.v432;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V433') AND 'V433' = r.column_name
         THEN
            oldval := :old.v433;
            newval := :new.v433;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V434') AND 'V434' = r.column_name
         THEN
            oldval := :old.v434;
            newval := :new.v434;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V435') AND 'V435' = r.column_name
         THEN
            oldval := :old.v435;
            newval := :new.v435;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V436') AND 'V436' = r.column_name
         THEN
            oldval := :old.v436;
            newval := :new.v436;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V437') AND 'V437' = r.column_name
         THEN
            oldval := :old.v437;
            newval := :new.v437;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V438') AND 'V438' = r.column_name
         THEN
            oldval := :old.v438;
            newval := :new.v438;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V439') AND 'V439' = r.column_name
         THEN
            oldval := :old.v439;
            newval := :new.v439;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V440') AND 'V440' = r.column_name
         THEN
            oldval := :old.v440;
            newval := :new.v440;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V441') AND 'V441' = r.column_name
         THEN
            oldval := :old.v441;
            newval := :new.v441;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V442') AND 'V442' = r.column_name
         THEN
            oldval := :old.v442;
            newval := :new.v442;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V443') AND 'V443' = r.column_name
         THEN
            oldval := :old.v443;
            newval := :new.v443;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V444') AND 'V444' = r.column_name
         THEN
            oldval := :old.v444;
            newval := :new.v444;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V445') AND 'V445' = r.column_name
         THEN
            oldval := :old.v445;
            newval := :new.v445;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V446') AND 'V446' = r.column_name
         THEN
            oldval := :old.v446;
            newval := :new.v446;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V447') AND 'V447' = r.column_name
         THEN
            oldval := :old.v447;
            newval := :new.v447;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V448') AND 'V448' = r.column_name
         THEN
            oldval := :old.v448;
            newval := :new.v448;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V449') AND 'V449' = r.column_name
         THEN
            oldval := :old.v449;
            newval := :new.v449;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V450') AND 'V450' = r.column_name
         THEN
            oldval := :old.v450;
            newval := :new.v450;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V451') AND 'V451' = r.column_name
         THEN
            oldval := :old.v451;
            newval := :new.v451;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V452') AND 'V452' = r.column_name
         THEN
            oldval := :old.v452;
            newval := :new.v452;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V453') AND 'V453' = r.column_name
         THEN
            oldval := :old.v453;
            newval := :new.v453;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V454') AND 'V454' = r.column_name
         THEN
            oldval := :old.v454;
            newval := :new.v454;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V455') AND 'V455' = r.column_name
         THEN
            oldval := :old.v455;
            newval := :new.v455;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V456') AND 'V456' = r.column_name
         THEN
            oldval := :old.v456;
            newval := :new.v456;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V457') AND 'V457' = r.column_name
         THEN
            oldval := :old.v457;
            newval := :new.v457;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V458') AND 'V458' = r.column_name
         THEN
            oldval := :old.v458;
            newval := :new.v458;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V459') AND 'V459' = r.column_name
         THEN
            oldval := :old.v459;
            newval := :new.v459;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V460') AND 'V460' = r.column_name
         THEN
            oldval := :old.v460;
            newval := :new.v460;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V461') AND 'V461' = r.column_name
         THEN
            oldval := :old.v461;
            newval := :new.v461;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V462') AND 'V462' = r.column_name
         THEN
            oldval := :old.v462;
            newval := :new.v462;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V463') AND 'V463' = r.column_name
         THEN
            oldval := :old.v463;
            newval := :new.v463;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V464') AND 'V464' = r.column_name
         THEN
            oldval := :old.v464;
            newval := :new.v464;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V465') AND 'V465' = r.column_name
         THEN
            oldval := :old.v465;
            newval := :new.v465;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V466') AND 'V466' = r.column_name
         THEN
            oldval := :old.v466;
            newval := :new.v466;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V467') AND 'V467' = r.column_name
         THEN
            oldval := :old.v467;
            newval := :new.v467;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V468') AND 'V468' = r.column_name
         THEN
            oldval := :old.v468;
            newval := :new.v468;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V469') AND 'V469' = r.column_name
         THEN
            oldval := :old.v469;
            newval := :new.v469;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V470') AND 'V470' = r.column_name
         THEN
            oldval := :old.v470;
            newval := :new.v470;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V471') AND 'V471' = r.column_name
         THEN
            oldval := :old.v471;
            newval := :new.v471;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V472') AND 'V472' = r.column_name
         THEN
            oldval := :old.v472;
            newval := :new.v472;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V473') AND 'V473' = r.column_name
         THEN
            oldval := :old.v473;
            newval := :new.v473;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V474') AND 'V474' = r.column_name
         THEN
            oldval := :old.v474;
            newval := :new.v474;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V475') AND 'V475' = r.column_name
         THEN
            oldval := :old.v475;
            newval := :new.v475;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V476') AND 'V476' = r.column_name
         THEN
            oldval := :old.v476;
            newval := :new.v476;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V477') AND 'V477' = r.column_name
         THEN
            oldval := :old.v477;
            newval := :new.v477;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V478') AND 'V478' = r.column_name
         THEN
            oldval := :old.v478;
            newval := :new.v478;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V479') AND 'V479' = r.column_name
         THEN
            oldval := :old.v479;
            newval := :new.v479;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V480') AND 'V480' = r.column_name
         THEN
            oldval := :old.v480;
            newval := :new.v480;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V481') AND 'V481' = r.column_name
         THEN
            oldval := :old.v481;
            newval := :new.v481;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V482') AND 'V482' = r.column_name
         THEN
            oldval := :old.v482;
            newval := :new.v482;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V483') AND 'V483' = r.column_name
         THEN
            oldval := :old.v483;
            newval := :new.v483;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V484') AND 'V484' = r.column_name
         THEN
            oldval := :old.v484;
            newval := :new.v484;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V485') AND 'V485' = r.column_name
         THEN
            oldval := :old.v485;
            newval := :new.v485;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V486') AND 'V486' = r.column_name
         THEN
            oldval := :old.v486;
            newval := :new.v486;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V487') AND 'V487' = r.column_name
         THEN
            oldval := :old.v487;
            newval := :new.v487;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V488') AND 'V488' = r.column_name
         THEN
            oldval := :old.v488;
            newval := :new.v488;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V489') AND 'V489' = r.column_name
         THEN
            oldval := :old.v489;
            newval := :new.v489;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V490') AND 'V490' = r.column_name
         THEN
            oldval := :old.v490;
            newval := :new.v490;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V491') AND 'V491' = r.column_name
         THEN
            oldval := :old.v491;
            newval := :new.v491;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V492') AND 'V492' = r.column_name
         THEN
            oldval := :old.v492;
            newval := :new.v492;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V493') AND 'V493' = r.column_name
         THEN
            oldval := :old.v493;
            newval := :new.v493;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V494') AND 'V494' = r.column_name
         THEN
            oldval := :old.v494;
            newval := :new.v494;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V495') AND 'V495' = r.column_name
         THEN
            oldval := :old.v495;
            newval := :new.v495;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V496') AND 'V496' = r.column_name
         THEN
            oldval := :old.v496;
            newval := :new.v496;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V497') AND 'V497' = r.column_name
         THEN
            oldval := :old.v497;
            newval := :new.v497;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V498') AND 'V498' = r.column_name
         THEN
            oldval := :old.v498;
            newval := :new.v498;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V499') AND 'V499' = r.column_name
         THEN
            oldval := :old.v499;
            newval := :new.v499;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V500') AND 'V500' = r.column_name
         THEN
            oldval := :old.v500;
            newval := :new.v500;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V501') AND 'V501' = r.column_name
         THEN
            oldval := :old.v501;
            newval := :new.v501;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V502') AND 'V502' = r.column_name
         THEN
            oldval := :old.v502;
            newval := :new.v502;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V503') AND 'V503' = r.column_name
         THEN
            oldval := :old.v503;
            newval := :new.v503;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V504') AND 'V504' = r.column_name
         THEN
            oldval := :old.v504;
            newval := :new.v504;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V505') AND 'V505' = r.column_name
         THEN
            oldval := :old.v505;
            newval := :new.v505;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V506') AND 'V506' = r.column_name
         THEN
            oldval := :old.v506;
            newval := :new.v506;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V507') AND 'V507' = r.column_name
         THEN
            oldval := :old.v507;
            newval := :new.v507;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V508') AND 'V508' = r.column_name
         THEN
            oldval := :old.v508;
            newval := :new.v508;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V509') AND 'V509' = r.column_name
         THEN
            oldval := :old.v509;
            newval := :new.v509;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V510') AND 'V510' = r.column_name
         THEN
            oldval := :old.v510;
            newval := :new.v510;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V511') AND 'V511' = r.column_name
         THEN
            oldval := :old.v511;
            newval := :new.v511;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V512') AND 'V512' = r.column_name
         THEN
            oldval := :old.v512;
            newval := :new.v512;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V513') AND 'V513' = r.column_name
         THEN
            oldval := :old.v513;
            newval := :new.v513;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V514') AND 'V514' = r.column_name
         THEN
            oldval := :old.v514;
            newval := :new.v514;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V515') AND 'V515' = r.column_name
         THEN
            oldval := :old.v515;
            newval := :new.v515;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V516') AND 'V516' = r.column_name
         THEN
            oldval := :old.v516;
            newval := :new.v516;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V517') AND 'V517' = r.column_name
         THEN
            oldval := :old.v517;
            newval := :new.v517;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V518') AND 'V518' = r.column_name
         THEN
            oldval := :old.v518;
            newval := :new.v518;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V519') AND 'V519' = r.column_name
         THEN
            oldval := :old.v519;
            newval := :new.v519;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V520') AND 'V520' = r.column_name
         THEN
            oldval := :old.v520;
            newval := :new.v520;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V521') AND 'V521' = r.column_name
         THEN
            oldval := :old.v521;
            newval := :new.v521;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V522') AND 'V522' = r.column_name
         THEN
            oldval := :old.v522;
            newval := :new.v522;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V523') AND 'V523' = r.column_name
         THEN
            oldval := :old.v523;
            newval := :new.v523;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V524') AND 'V524' = r.column_name
         THEN
            oldval := :old.v524;
            newval := :new.v524;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V525') AND 'V525' = r.column_name
         THEN
            oldval := :old.v525;
            newval := :new.v525;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V526') AND 'V526' = r.column_name
         THEN
            oldval := :old.v526;
            newval := :new.v526;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V527') AND 'V527' = r.column_name
         THEN
            oldval := :old.v527;
            newval := :new.v527;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V528') AND 'V528' = r.column_name
         THEN
            oldval := :old.v528;
            newval := :new.v528;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V529') AND 'V529' = r.column_name
         THEN
            oldval := :old.v529;
            newval := :new.v529;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V530') AND 'V530' = r.column_name
         THEN
            oldval := :old.v530;
            newval := :new.v530;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V531') AND 'V531' = r.column_name
         THEN
            oldval := :old.v531;
            newval := :new.v531;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V532') AND 'V532' = r.column_name
         THEN
            oldval := :old.v532;
            newval := :new.v532;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V533') AND 'V533' = r.column_name
         THEN
            oldval := :old.v533;
            newval := :new.v533;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V534') AND 'V534' = r.column_name
         THEN
            oldval := :old.v534;
            newval := :new.v534;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V535') AND 'V535' = r.column_name
         THEN
            oldval := :old.v535;
            newval := :new.v535;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V536') AND 'V536' = r.column_name
         THEN
            oldval := :old.v536;
            newval := :new.v536;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V537') AND 'V537' = r.column_name
         THEN
            oldval := :old.v537;
            newval := :new.v537;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V538') AND 'V538' = r.column_name
         THEN
            oldval := :old.v538;
            newval := :new.v538;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V539') AND 'V539' = r.column_name
         THEN
            oldval := :old.v539;
            newval := :new.v539;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V540') AND 'V540' = r.column_name
         THEN
            oldval := :old.v540;
            newval := :new.v540;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V541') AND 'V541' = r.column_name
         THEN
            oldval := :old.v541;
            newval := :new.v541;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V542') AND 'V542' = r.column_name
         THEN
            oldval := :old.v542;
            newval := :new.v542;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V543') AND 'V543' = r.column_name
         THEN
            oldval := :old.v543;
            newval := :new.v543;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V544') AND 'V544' = r.column_name
         THEN
            oldval := :old.v544;
            newval := :new.v544;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V545') AND 'V545' = r.column_name
         THEN
            oldval := :old.v545;
            newval := :new.v545;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V546') AND 'V546' = r.column_name
         THEN
            oldval := :old.v546;
            newval := :new.v546;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V547') AND 'V547' = r.column_name
         THEN
            oldval := :old.v547;
            newval := :new.v547;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V548') AND 'V548' = r.column_name
         THEN
            oldval := :old.v548;
            newval := :new.v548;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V549') AND 'V549' = r.column_name
         THEN
            oldval := :old.v549;
            newval := :new.v549;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V550') AND 'V550' = r.column_name
         THEN
            oldval := :old.v550;
            newval := :new.v550;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V551') AND 'V551' = r.column_name
         THEN
            oldval := :old.v551;
            newval := :new.v551;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V552') AND 'V552' = r.column_name
         THEN
            oldval := :old.v552;
            newval := :new.v552;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V553') AND 'V553' = r.column_name
         THEN
            oldval := :old.v553;
            newval := :new.v553;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V554') AND 'V554' = r.column_name
         THEN
            oldval := :old.v554;
            newval := :new.v554;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V555') AND 'V555' = r.column_name
         THEN
            oldval := :old.v555;
            newval := :new.v555;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V556') AND 'V556' = r.column_name
         THEN
            oldval := :old.v556;
            newval := :new.v556;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V557') AND 'V557' = r.column_name
         THEN
            oldval := :old.v557;
            newval := :new.v557;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V558') AND 'V558' = r.column_name
         THEN
            oldval := :old.v558;
            newval := :new.v558;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V559') AND 'V559' = r.column_name
         THEN
            oldval := :old.v559;
            newval := :new.v559;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V560') AND 'V560' = r.column_name
         THEN
            oldval := :old.v560;
            newval := :new.v560;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V561') AND 'V561' = r.column_name
         THEN
            oldval := :old.v561;
            newval := :new.v561;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V562') AND 'V562' = r.column_name
         THEN
            oldval := :old.v562;
            newval := :new.v562;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V563') AND 'V563' = r.column_name
         THEN
            oldval := :old.v563;
            newval := :new.v563;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V564') AND 'V564' = r.column_name
         THEN
            oldval := :old.v564;
            newval := :new.v564;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V565') AND 'V565' = r.column_name
         THEN
            oldval := :old.v565;
            newval := :new.v565;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V566') AND 'V566' = r.column_name
         THEN
            oldval := :old.v566;
            newval := :new.v566;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V567') AND 'V567' = r.column_name
         THEN
            oldval := :old.v567;
            newval := :new.v567;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V568') AND 'V568' = r.column_name
         THEN
            oldval := :old.v568;
            newval := :new.v568;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V569') AND 'V569' = r.column_name
         THEN
            oldval := :old.v569;
            newval := :new.v569;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V570') AND 'V570' = r.column_name
         THEN
            oldval := :old.v570;
            newval := :new.v570;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V571') AND 'V571' = r.column_name
         THEN
            oldval := :old.v571;
            newval := :new.v571;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V572') AND 'V572' = r.column_name
         THEN
            oldval := :old.v572;
            newval := :new.v572;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V573') AND 'V573' = r.column_name
         THEN
            oldval := :old.v573;
            newval := :new.v573;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V574') AND 'V574' = r.column_name
         THEN
            oldval := :old.v574;
            newval := :new.v574;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V575') AND 'V575' = r.column_name
         THEN
            oldval := :old.v575;
            newval := :new.v575;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V576') AND 'V576' = r.column_name
         THEN
            oldval := :old.v576;
            newval := :new.v576;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V577') AND 'V577' = r.column_name
         THEN
            oldval := :old.v577;
            newval := :new.v577;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V578') AND 'V578' = r.column_name
         THEN
            oldval := :old.v578;
            newval := :new.v578;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V579') AND 'V579' = r.column_name
         THEN
            oldval := :old.v579;
            newval := :new.v579;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V580') AND 'V580' = r.column_name
         THEN
            oldval := :old.v580;
            newval := :new.v580;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V581') AND 'V581' = r.column_name
         THEN
            oldval := :old.v581;
            newval := :new.v581;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V582') AND 'V582' = r.column_name
         THEN
            oldval := :old.v582;
            newval := :new.v582;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V583') AND 'V583' = r.column_name
         THEN
            oldval := :old.v583;
            newval := :new.v583;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V584') AND 'V584' = r.column_name
         THEN
            oldval := :old.v584;
            newval := :new.v584;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V585') AND 'V585' = r.column_name
         THEN
            oldval := :old.v585;
            newval := :new.v585;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V586') AND 'V586' = r.column_name
         THEN
            oldval := :old.v586;
            newval := :new.v586;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V587') AND 'V587' = r.column_name
         THEN
            oldval := :old.v587;
            newval := :new.v587;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V588') AND 'V588' = r.column_name
         THEN
            oldval := :old.v588;
            newval := :new.v588;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V589') AND 'V589' = r.column_name
         THEN
            oldval := :old.v589;
            newval := :new.v589;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V590') AND 'V590' = r.column_name
         THEN
            oldval := :old.v590;
            newval := :new.v590;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V591') AND 'V591' = r.column_name
         THEN
            oldval := :old.v591;
            newval := :new.v591;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V592') AND 'V592' = r.column_name
         THEN
            oldval := :old.v592;
            newval := :new.v592;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V593') AND 'V593' = r.column_name
         THEN
            oldval := :old.v593;
            newval := :new.v593;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V594') AND 'V594' = r.column_name
         THEN
            oldval := :old.v594;
            newval := :new.v594;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V595') AND 'V595' = r.column_name
         THEN
            oldval := :old.v595;
            newval := :new.v595;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V596') AND 'V596' = r.column_name
         THEN
            oldval := :old.v596;
            newval := :new.v596;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V597') AND 'V597' = r.column_name
         THEN
            oldval := :old.v597;
            newval := :new.v597;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V598') AND 'V598' = r.column_name
         THEN
            oldval := :old.v598;
            newval := :new.v598;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V599') AND 'V599' = r.column_name
         THEN
            oldval := :old.v599;
            newval := :new.v599;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V600') AND 'V600' = r.column_name
         THEN
            oldval := :old.v600;
            newval := :new.v600;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V601') AND 'V601' = r.column_name
         THEN
            oldval := :old.v601;
            newval := :new.v601;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V602') AND 'V602' = r.column_name
         THEN
            oldval := :old.v602;
            newval := :new.v602;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V603') AND 'V603' = r.column_name
         THEN
            oldval := :old.v603;
            newval := :new.v603;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V604') AND 'V604' = r.column_name
         THEN
            oldval := :old.v604;
            newval := :new.v604;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V605') AND 'V605' = r.column_name
         THEN
            oldval := :old.v605;
            newval := :new.v605;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V606') AND 'V606' = r.column_name
         THEN
            oldval := :old.v606;
            newval := :new.v606;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V607') AND 'V607' = r.column_name
         THEN
            oldval := :old.v607;
            newval := :new.v607;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V608') AND 'V608' = r.column_name
         THEN
            oldval := :old.v608;
            newval := :new.v608;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V609') AND 'V609' = r.column_name
         THEN
            oldval := :old.v609;
            newval := :new.v609;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V610') AND 'V610' = r.column_name
         THEN
            oldval := :old.v610;
            newval := :new.v610;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V611') AND 'V611' = r.column_name
         THEN
            oldval := :old.v611;
            newval := :new.v611;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V612') AND 'V612' = r.column_name
         THEN
            oldval := :old.v612;
            newval := :new.v612;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V613') AND 'V613' = r.column_name
         THEN
            oldval := :old.v613;
            newval := :new.v613;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V614') AND 'V614' = r.column_name
         THEN
            oldval := :old.v614;
            newval := :new.v614;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V615') AND 'V615' = r.column_name
         THEN
            oldval := :old.v615;
            newval := :new.v615;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V616') AND 'V616' = r.column_name
         THEN
            oldval := :old.v616;
            newval := :new.v616;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V617') AND 'V617' = r.column_name
         THEN
            oldval := :old.v617;
            newval := :new.v617;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V618') AND 'V618' = r.column_name
         THEN
            oldval := :old.v618;
            newval := :new.v618;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V619') AND 'V619' = r.column_name
         THEN
            oldval := :old.v619;
            newval := :new.v619;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V620') AND 'V620' = r.column_name
         THEN
            oldval := :old.v620;
            newval := :new.v620;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V621') AND 'V621' = r.column_name
         THEN
            oldval := :old.v621;
            newval := :new.v621;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V622') AND 'V622' = r.column_name
         THEN
            oldval := :old.v622;
            newval := :new.v622;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V623') AND 'V623' = r.column_name
         THEN
            oldval := :old.v623;
            newval := :new.v623;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V624') AND 'V624' = r.column_name
         THEN
            oldval := :old.v624;
            newval := :new.v624;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V625') AND 'V625' = r.column_name
         THEN
            oldval := :old.v625;
            newval := :new.v625;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V626') AND 'V626' = r.column_name
         THEN
            oldval := :old.v626;
            newval := :new.v626;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V627') AND 'V627' = r.column_name
         THEN
            oldval := :old.v627;
            newval := :new.v627;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V628') AND 'V628' = r.column_name
         THEN
            oldval := :old.v628;
            newval := :new.v628;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V629') AND 'V629' = r.column_name
         THEN
            oldval := :old.v629;
            newval := :new.v629;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V630') AND 'V630' = r.column_name
         THEN
            oldval := :old.v630;
            newval := :new.v630;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V631') AND 'V631' = r.column_name
         THEN
            oldval := :old.v631;
            newval := :new.v631;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V632') AND 'V632' = r.column_name
         THEN
            oldval := :old.v632;
            newval := :new.v632;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V633') AND 'V633' = r.column_name
         THEN
            oldval := :old.v633;
            newval := :new.v633;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V634') AND 'V634' = r.column_name
         THEN
            oldval := :old.v634;
            newval := :new.v634;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V635') AND 'V635' = r.column_name
         THEN
            oldval := :old.v635;
            newval := :new.v635;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V636') AND 'V636' = r.column_name
         THEN
            oldval := :old.v636;
            newval := :new.v636;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V637') AND 'V637' = r.column_name
         THEN
            oldval := :old.v637;
            newval := :new.v637;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V638') AND 'V638' = r.column_name
         THEN
            oldval := :old.v638;
            newval := :new.v638;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V639') AND 'V639' = r.column_name
         THEN
            oldval := :old.v639;
            newval := :new.v639;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V640') AND 'V640' = r.column_name
         THEN
            oldval := :old.v640;
            newval := :new.v640;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V641') AND 'V641' = r.column_name
         THEN
            oldval := :old.v641;
            newval := :new.v641;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V642') AND 'V642' = r.column_name
         THEN
            oldval := :old.v642;
            newval := :new.v642;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V643') AND 'V643' = r.column_name
         THEN
            oldval := :old.v643;
            newval := :new.v643;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V644') AND 'V644' = r.column_name
         THEN
            oldval := :old.v644;
            newval := :new.v644;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V645') AND 'V645' = r.column_name
         THEN
            oldval := :old.v645;
            newval := :new.v645;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V646') AND 'V646' = r.column_name
         THEN
            oldval := :old.v646;
            newval := :new.v646;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V647') AND 'V647' = r.column_name
         THEN
            oldval := :old.v647;
            newval := :new.v647;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V648') AND 'V648' = r.column_name
         THEN
            oldval := :old.v648;
            newval := :new.v648;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V649') AND 'V649' = r.column_name
         THEN
            oldval := :old.v649;
            newval := :new.v649;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V650') AND 'V650' = r.column_name
         THEN
            oldval := :old.v650;
            newval := :new.v650;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V651') AND 'V651' = r.column_name
         THEN
            oldval := :old.v651;
            newval := :new.v651;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V652') AND 'V652' = r.column_name
         THEN
            oldval := :old.v652;
            newval := :new.v652;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V653') AND 'V653' = r.column_name
         THEN
            oldval := :old.v653;
            newval := :new.v653;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V654') AND 'V654' = r.column_name
         THEN
            oldval := :old.v654;
            newval := :new.v654;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V655') AND 'V655' = r.column_name
         THEN
            oldval := :old.v655;
            newval := :new.v655;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V656') AND 'V656' = r.column_name
         THEN
            oldval := :old.v656;
            newval := :new.v656;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V657') AND 'V657' = r.column_name
         THEN
            oldval := :old.v657;
            newval := :new.v657;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V658') AND 'V658' = r.column_name
         THEN
            oldval := :old.v658;
            newval := :new.v658;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V659') AND 'V659' = r.column_name
         THEN
            oldval := :old.v659;
            newval := :new.v659;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V660') AND 'V660' = r.column_name
         THEN
            oldval := :old.v660;
            newval := :new.v660;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V661') AND 'V661' = r.column_name
         THEN
            oldval := :old.v661;
            newval := :new.v661;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V662') AND 'V662' = r.column_name
         THEN
            oldval := :old.v662;
            newval := :new.v662;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V663') AND 'V663' = r.column_name
         THEN
            oldval := :old.v663;
            newval := :new.v663;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V664') AND 'V664' = r.column_name
         THEN
            oldval := :old.v664;
            newval := :new.v664;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V665') AND 'V665' = r.column_name
         THEN
            oldval := :old.v665;
            newval := :new.v665;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V666') AND 'V666' = r.column_name
         THEN
            oldval := :old.v666;
            newval := :new.v666;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V667') AND 'V667' = r.column_name
         THEN
            oldval := :old.v667;
            newval := :new.v667;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V668') AND 'V668' = r.column_name
         THEN
            oldval := :old.v668;
            newval := :new.v668;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V669') AND 'V669' = r.column_name
         THEN
            oldval := :old.v669;
            newval := :new.v669;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V670') AND 'V670' = r.column_name
         THEN
            oldval := :old.v670;
            newval := :new.v670;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V671') AND 'V671' = r.column_name
         THEN
            oldval := :old.v671;
            newval := :new.v671;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V672') AND 'V672' = r.column_name
         THEN
            oldval := :old.v672;
            newval := :new.v672;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V673') AND 'V673' = r.column_name
         THEN
            oldval := :old.v673;
            newval := :new.v673;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V674') AND 'V674' = r.column_name
         THEN
            oldval := :old.v674;
            newval := :new.v674;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V675') AND 'V675' = r.column_name
         THEN
            oldval := :old.v675;
            newval := :new.v675;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V676') AND 'V676' = r.column_name
         THEN
            oldval := :old.v676;
            newval := :new.v676;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V677') AND 'V677' = r.column_name
         THEN
            oldval := :old.v677;
            newval := :new.v677;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V678') AND 'V678' = r.column_name
         THEN
            oldval := :old.v678;
            newval := :new.v678;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V679') AND 'V679' = r.column_name
         THEN
            oldval := :old.v679;
            newval := :new.v679;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V680') AND 'V680' = r.column_name
         THEN
            oldval := :old.v680;
            newval := :new.v680;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V681') AND 'V681' = r.column_name
         THEN
            oldval := :old.v681;
            newval := :new.v681;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V682') AND 'V682' = r.column_name
         THEN
            oldval := :old.v682;
            newval := :new.v682;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V683') AND 'V683' = r.column_name
         THEN
            oldval := :old.v683;
            newval := :new.v683;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V684') AND 'V684' = r.column_name
         THEN
            oldval := :old.v684;
            newval := :new.v684;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V685') AND 'V685' = r.column_name
         THEN
            oldval := :old.v685;
            newval := :new.v685;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V686') AND 'V686' = r.column_name
         THEN
            oldval := :old.v686;
            newval := :new.v686;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V687') AND 'V687' = r.column_name
         THEN
            oldval := :old.v687;
            newval := :new.v687;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V688') AND 'V688' = r.column_name
         THEN
            oldval := :old.v688;
            newval := :new.v688;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V689') AND 'V689' = r.column_name
         THEN
            oldval := :old.v689;
            newval := :new.v689;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V690') AND 'V690' = r.column_name
         THEN
            oldval := :old.v690;
            newval := :new.v690;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V691') AND 'V691' = r.column_name
         THEN
            oldval := :old.v691;
            newval := :new.v691;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V692') AND 'V692' = r.column_name
         THEN
            oldval := :old.v692;
            newval := :new.v692;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V693') AND 'V693' = r.column_name
         THEN
            oldval := :old.v693;
            newval := :new.v693;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V694') AND 'V694' = r.column_name
         THEN
            oldval := :old.v694;
            newval := :new.v694;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V695') AND 'V695' = r.column_name
         THEN
            oldval := :old.v695;
            newval := :new.v695;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V696') AND 'V696' = r.column_name
         THEN
            oldval := :old.v696;
            newval := :new.v696;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V697') AND 'V697' = r.column_name
         THEN
            oldval := :old.v697;
            newval := :new.v697;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V698') AND 'V698' = r.column_name
         THEN
            oldval := :old.v698;
            newval := :new.v698;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V699') AND 'V699' = r.column_name
         THEN
            oldval := :old.v699;
            newval := :new.v699;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V700') AND 'V700' = r.column_name
         THEN
            oldval := :old.v700;
            newval := :new.v700;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V701') AND 'V701' = r.column_name
         THEN
            oldval := :old.v701;
            newval := :new.v701;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V702') AND 'V702' = r.column_name
         THEN
            oldval := :old.v702;
            newval := :new.v702;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V703') AND 'V703' = r.column_name
         THEN
            oldval := :old.v703;
            newval := :new.v703;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V704') AND 'V704' = r.column_name
         THEN
            oldval := :old.v704;
            newval := :new.v704;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V705') AND 'V705' = r.column_name
         THEN
            oldval := :old.v705;
            newval := :new.v705;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V706') AND 'V706' = r.column_name
         THEN
            oldval := :old.v706;
            newval := :new.v706;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V707') AND 'V707' = r.column_name
         THEN
            oldval := :old.v707;
            newval := :new.v707;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V708') AND 'V708' = r.column_name
         THEN
            oldval := :old.v708;
            newval := :new.v708;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V709') AND 'V709' = r.column_name
         THEN
            oldval := :old.v709;
            newval := :new.v709;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V710') AND 'V710' = r.column_name
         THEN
            oldval := :old.v710;
            newval := :new.v710;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V711') AND 'V711' = r.column_name
         THEN
            oldval := :old.v711;
            newval := :new.v711;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V712') AND 'V712' = r.column_name
         THEN
            oldval := :old.v712;
            newval := :new.v712;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V713') AND 'V713' = r.column_name
         THEN
            oldval := :old.v713;
            newval := :new.v713;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V714') AND 'V714' = r.column_name
         THEN
            oldval := :old.v714;
            newval := :new.v714;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V715') AND 'V715' = r.column_name
         THEN
            oldval := :old.v715;
            newval := :new.v715;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V716') AND 'V716' = r.column_name
         THEN
            oldval := :old.v716;
            newval := :new.v716;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V717') AND 'V717' = r.column_name
         THEN
            oldval := :old.v717;
            newval := :new.v717;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V718') AND 'V718' = r.column_name
         THEN
            oldval := :old.v718;
            newval := :new.v718;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V719') AND 'V719' = r.column_name
         THEN
            oldval := :old.v719;
            newval := :new.v719;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V720') AND 'V720' = r.column_name
         THEN
            oldval := :old.v720;
            newval := :new.v720;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V721') AND 'V721' = r.column_name
         THEN
            oldval := :old.v721;
            newval := :new.v721;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V722') AND 'V722' = r.column_name
         THEN
            oldval := :old.v722;
            newval := :new.v722;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V723') AND 'V723' = r.column_name
         THEN
            oldval := :old.v723;
            newval := :new.v723;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V724') AND 'V724' = r.column_name
         THEN
            oldval := :old.v724;
            newval := :new.v724;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V725') AND 'V725' = r.column_name
         THEN
            oldval := :old.v725;
            newval := :new.v725;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V726') AND 'V726' = r.column_name
         THEN
            oldval := :old.v726;
            newval := :new.v726;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V727') AND 'V727' = r.column_name
         THEN
            oldval := :old.v727;
            newval := :new.v727;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V728') AND 'V728' = r.column_name
         THEN
            oldval := :old.v728;
            newval := :new.v728;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V729') AND 'V729' = r.column_name
         THEN
            oldval := :old.v729;
            newval := :new.v729;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V730') AND 'V730' = r.column_name
         THEN
            oldval := :old.v730;
            newval := :new.v730;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V731') AND 'V731' = r.column_name
         THEN
            oldval := :old.v731;
            newval := :new.v731;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V732') AND 'V732' = r.column_name
         THEN
            oldval := :old.v732;
            newval := :new.v732;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V733') AND 'V733' = r.column_name
         THEN
            oldval := :old.v733;
            newval := :new.v733;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V734') AND 'V734' = r.column_name
         THEN
            oldval := :old.v734;
            newval := :new.v734;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V735') AND 'V735' = r.column_name
         THEN
            oldval := :old.v735;
            newval := :new.v735;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V736') AND 'V736' = r.column_name
         THEN
            oldval := :old.v736;
            newval := :new.v736;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V737') AND 'V737' = r.column_name
         THEN
            oldval := :old.v737;
            newval := :new.v737;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V738') AND 'V738' = r.column_name
         THEN
            oldval := :old.v738;
            newval := :new.v738;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V739') AND 'V739' = r.column_name
         THEN
            oldval := :old.v739;
            newval := :new.v739;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V740') AND 'V740' = r.column_name
         THEN
            oldval := :old.v740;
            newval := :new.v740;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V741') AND 'V741' = r.column_name
         THEN
            oldval := :old.v741;
            newval := :new.v741;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V742') AND 'V742' = r.column_name
         THEN
            oldval := :old.v742;
            newval := :new.v742;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V743') AND 'V743' = r.column_name
         THEN
            oldval := :old.v743;
            newval := :new.v743;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V744') AND 'V744' = r.column_name
         THEN
            oldval := :old.v744;
            newval := :new.v744;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V745') AND 'V745' = r.column_name
         THEN
            oldval := :old.v745;
            newval := :new.v745;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V746') AND 'V746' = r.column_name
         THEN
            oldval := :old.v746;
            newval := :new.v746;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V747') AND 'V747' = r.column_name
         THEN
            oldval := :old.v747;
            newval := :new.v747;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V748') AND 'V748' = r.column_name
         THEN
            oldval := :old.v748;
            newval := :new.v748;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V749') AND 'V749' = r.column_name
         THEN
            oldval := :old.v749;
            newval := :new.v749;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V750') AND 'V750' = r.column_name
         THEN
            oldval := :old.v750;
            newval := :new.v750;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V751') AND 'V751' = r.column_name
         THEN
            oldval := :old.v751;
            newval := :new.v751;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V752') AND 'V752' = r.column_name
         THEN
            oldval := :old.v752;
            newval := :new.v752;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V753') AND 'V753' = r.column_name
         THEN
            oldval := :old.v753;
            newval := :new.v753;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V754') AND 'V754' = r.column_name
         THEN
            oldval := :old.v754;
            newval := :new.v754;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V755') AND 'V755' = r.column_name
         THEN
            oldval := :old.v755;
            newval := :new.v755;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V756') AND 'V756' = r.column_name
         THEN
            oldval := :old.v756;
            newval := :new.v756;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V757') AND 'V757' = r.column_name
         THEN
            oldval := :old.v757;
            newval := :new.v757;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V758') AND 'V758' = r.column_name
         THEN
            oldval := :old.v758;
            newval := :new.v758;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V759') AND 'V759' = r.column_name
         THEN
            oldval := :old.v759;
            newval := :new.v759;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V760') AND 'V760' = r.column_name
         THEN
            oldval := :old.v760;
            newval := :new.v760;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V761') AND 'V761' = r.column_name
         THEN
            oldval := :old.v761;
            newval := :new.v761;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V762') AND 'V762' = r.column_name
         THEN
            oldval := :old.v762;
            newval := :new.v762;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V763') AND 'V763' = r.column_name
         THEN
            oldval := :old.v763;
            newval := :new.v763;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V764') AND 'V764' = r.column_name
         THEN
            oldval := :old.v764;
            newval := :new.v764;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V765') AND 'V765' = r.column_name
         THEN
            oldval := :old.v765;
            newval := :new.v765;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V766') AND 'V766' = r.column_name
         THEN
            oldval := :old.v766;
            newval := :new.v766;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V767') AND 'V767' = r.column_name
         THEN
            oldval := :old.v767;
            newval := :new.v767;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V768') AND 'V768' = r.column_name
         THEN
            oldval := :old.v768;
            newval := :new.v768;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V769') AND 'V769' = r.column_name
         THEN
            oldval := :old.v769;
            newval := :new.v769;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V770') AND 'V770' = r.column_name
         THEN
            oldval := :old.v770;
            newval := :new.v770;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V771') AND 'V771' = r.column_name
         THEN
            oldval := :old.v771;
            newval := :new.v771;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V772') AND 'V772' = r.column_name
         THEN
            oldval := :old.v772;
            newval := :new.v772;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V773') AND 'V773' = r.column_name
         THEN
            oldval := :old.v773;
            newval := :new.v773;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V774') AND 'V774' = r.column_name
         THEN
            oldval := :old.v774;
            newval := :new.v774;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V775') AND 'V775' = r.column_name
         THEN
            oldval := :old.v775;
            newval := :new.v775;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V776') AND 'V776' = r.column_name
         THEN
            oldval := :old.v776;
            newval := :new.v776;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V777') AND 'V777' = r.column_name
         THEN
            oldval := :old.v777;
            newval := :new.v777;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V778') AND 'V778' = r.column_name
         THEN
            oldval := :old.v778;
            newval := :new.v778;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V779') AND 'V779' = r.column_name
         THEN
            oldval := :old.v779;
            newval := :new.v779;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V780') AND 'V780' = r.column_name
         THEN
            oldval := :old.v780;
            newval := :new.v780;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V781') AND 'V781' = r.column_name
         THEN
            oldval := :old.v781;
            newval := :new.v781;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V782') AND 'V782' = r.column_name
         THEN
            oldval := :old.v782;
            newval := :new.v782;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V783') AND 'V783' = r.column_name
         THEN
            oldval := :old.v783;
            newval := :new.v783;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V784') AND 'V784' = r.column_name
         THEN
            oldval := :old.v784;
            newval := :new.v784;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V785') AND 'V785' = r.column_name
         THEN
            oldval := :old.v785;
            newval := :new.v785;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V786') AND 'V786' = r.column_name
         THEN
            oldval := :old.v786;
            newval := :new.v786;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V787') AND 'V787' = r.column_name
         THEN
            oldval := :old.v787;
            newval := :new.v787;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V788') AND 'V788' = r.column_name
         THEN
            oldval := :old.v788;
            newval := :new.v788;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V789') AND 'V789' = r.column_name
         THEN
            oldval := :old.v789;
            newval := :new.v789;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V790') AND 'V790' = r.column_name
         THEN
            oldval := :old.v790;
            newval := :new.v790;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V791') AND 'V791' = r.column_name
         THEN
            oldval := :old.v791;
            newval := :new.v791;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V792') AND 'V792' = r.column_name
         THEN
            oldval := :old.v792;
            newval := :new.v792;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V793') AND 'V793' = r.column_name
         THEN
            oldval := :old.v793;
            newval := :new.v793;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V794') AND 'V794' = r.column_name
         THEN
            oldval := :old.v794;
            newval := :new.v794;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V795') AND 'V795' = r.column_name
         THEN
            oldval := :old.v795;
            newval := :new.v795;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V796') AND 'V796' = r.column_name
         THEN
            oldval := :old.v796;
            newval := :new.v796;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V797') AND 'V797' = r.column_name
         THEN
            oldval := :old.v797;
            newval := :new.v797;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V798') AND 'V798' = r.column_name
         THEN
            oldval := :old.v798;
            newval := :new.v798;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V799') AND 'V799' = r.column_name
         THEN
            oldval := :old.v799;
            newval := :new.v799;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V800') AND 'V800' = r.column_name
         THEN
            oldval := :old.v800;
            newval := :new.v800;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V801') AND 'V801' = r.column_name
         THEN
            oldval := :old.v801;
            newval := :new.v801;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V802') AND 'V802' = r.column_name
         THEN
            oldval := :old.v802;
            newval := :new.v802;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V803') AND 'V803' = r.column_name
         THEN
            oldval := :old.v803;
            newval := :new.v803;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V804') AND 'V804' = r.column_name
         THEN
            oldval := :old.v804;
            newval := :new.v804;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V805') AND 'V805' = r.column_name
         THEN
            oldval := :old.v805;
            newval := :new.v805;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V806') AND 'V806' = r.column_name
         THEN
            oldval := :old.v806;
            newval := :new.v806;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V807') AND 'V807' = r.column_name
         THEN
            oldval := :old.v807;
            newval := :new.v807;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V808') AND 'V808' = r.column_name
         THEN
            oldval := :old.v808;
            newval := :new.v808;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V809') AND 'V809' = r.column_name
         THEN
            oldval := :old.v809;
            newval := :new.v809;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V810') AND 'V810' = r.column_name
         THEN
            oldval := :old.v810;
            newval := :new.v810;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V811') AND 'V811' = r.column_name
         THEN
            oldval := :old.v811;
            newval := :new.v811;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V812') AND 'V812' = r.column_name
         THEN
            oldval := :old.v812;
            newval := :new.v812;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V813') AND 'V813' = r.column_name
         THEN
            oldval := :old.v813;
            newval := :new.v813;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V814') AND 'V814' = r.column_name
         THEN
            oldval := :old.v814;
            newval := :new.v814;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V815') AND 'V815' = r.column_name
         THEN
            oldval := :old.v815;
            newval := :new.v815;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V816') AND 'V816' = r.column_name
         THEN
            oldval := :old.v816;
            newval := :new.v816;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V817') AND 'V817' = r.column_name
         THEN
            oldval := :old.v817;
            newval := :new.v817;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V818') AND 'V818' = r.column_name
         THEN
            oldval := :old.v818;
            newval := :new.v818;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V819') AND 'V819' = r.column_name
         THEN
            oldval := :old.v819;
            newval := :new.v819;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V820') AND 'V820' = r.column_name
         THEN
            oldval := :old.v820;
            newval := :new.v820;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V821') AND 'V821' = r.column_name
         THEN
            oldval := :old.v821;
            newval := :new.v821;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V822') AND 'V822' = r.column_name
         THEN
            oldval := :old.v822;
            newval := :new.v822;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V823') AND 'V823' = r.column_name
         THEN
            oldval := :old.v823;
            newval := :new.v823;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V824') AND 'V824' = r.column_name
         THEN
            oldval := :old.v824;
            newval := :new.v824;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V825') AND 'V825' = r.column_name
         THEN
            oldval := :old.v825;
            newval := :new.v825;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V826') AND 'V826' = r.column_name
         THEN
            oldval := :old.v826;
            newval := :new.v826;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V827') AND 'V827' = r.column_name
         THEN
            oldval := :old.v827;
            newval := :new.v827;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V828') AND 'V828' = r.column_name
         THEN
            oldval := :old.v828;
            newval := :new.v828;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V829') AND 'V829' = r.column_name
         THEN
            oldval := :old.v829;
            newval := :new.v829;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V830') AND 'V830' = r.column_name
         THEN
            oldval := :old.v830;
            newval := :new.v830;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V831') AND 'V831' = r.column_name
         THEN
            oldval := :old.v831;
            newval := :new.v831;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V832') AND 'V832' = r.column_name
         THEN
            oldval := :old.v832;
            newval := :new.v832;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V833') AND 'V833' = r.column_name
         THEN
            oldval := :old.v833;
            newval := :new.v833;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V834') AND 'V834' = r.column_name
         THEN
            oldval := :old.v834;
            newval := :new.v834;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V835') AND 'V835' = r.column_name
         THEN
            oldval := :old.v835;
            newval := :new.v835;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V836') AND 'V836' = r.column_name
         THEN
            oldval := :old.v836;
            newval := :new.v836;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V837') AND 'V837' = r.column_name
         THEN
            oldval := :old.v837;
            newval := :new.v837;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V838') AND 'V838' = r.column_name
         THEN
            oldval := :old.v838;
            newval := :new.v838;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V839') AND 'V839' = r.column_name
         THEN
            oldval := :old.v839;
            newval := :new.v839;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V840') AND 'V840' = r.column_name
         THEN
            oldval := :old.v840;
            newval := :new.v840;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V841') AND 'V841' = r.column_name
         THEN
            oldval := :old.v841;
            newval := :new.v841;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V842') AND 'V842' = r.column_name
         THEN
            oldval := :old.v842;
            newval := :new.v842;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V843') AND 'V843' = r.column_name
         THEN
            oldval := :old.v843;
            newval := :new.v843;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V844') AND 'V844' = r.column_name
         THEN
            oldval := :old.v844;
            newval := :new.v844;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V845') AND 'V845' = r.column_name
         THEN
            oldval := :old.v845;
            newval := :new.v845;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V846') AND 'V846' = r.column_name
         THEN
            oldval := :old.v846;
            newval := :new.v846;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V847') AND 'V847' = r.column_name
         THEN
            oldval := :old.v847;
            newval := :new.v847;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V848') AND 'V848' = r.column_name
         THEN
            oldval := :old.v848;
            newval := :new.v848;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V849') AND 'V849' = r.column_name
         THEN
            oldval := :old.v849;
            newval := :new.v849;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V850') AND 'V850' = r.column_name
         THEN
            oldval := :old.v850;
            newval := :new.v850;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V851') AND 'V851' = r.column_name
         THEN
            oldval := :old.v851;
            newval := :new.v851;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V852') AND 'V852' = r.column_name
         THEN
            oldval := :old.v852;
            newval := :new.v852;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V853') AND 'V853' = r.column_name
         THEN
            oldval := :old.v853;
            newval := :new.v853;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V854') AND 'V854' = r.column_name
         THEN
            oldval := :old.v854;
            newval := :new.v854;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V855') AND 'V855' = r.column_name
         THEN
            oldval := :old.v855;
            newval := :new.v855;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V856') AND 'V856' = r.column_name
         THEN
            oldval := :old.v856;
            newval := :new.v856;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V857') AND 'V857' = r.column_name
         THEN
            oldval := :old.v857;
            newval := :new.v857;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V858') AND 'V858' = r.column_name
         THEN
            oldval := :old.v858;
            newval := :new.v858;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V859') AND 'V859' = r.column_name
         THEN
            oldval := :old.v859;
            newval := :new.v859;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V860') AND 'V860' = r.column_name
         THEN
            oldval := :old.v860;
            newval := :new.v860;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V861') AND 'V861' = r.column_name
         THEN
            oldval := :old.v861;
            newval := :new.v861;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V862') AND 'V862' = r.column_name
         THEN
            oldval := :old.v862;
            newval := :new.v862;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V863') AND 'V863' = r.column_name
         THEN
            oldval := :old.v863;
            newval := :new.v863;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V864') AND 'V864' = r.column_name
         THEN
            oldval := :old.v864;
            newval := :new.v864;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V865') AND 'V865' = r.column_name
         THEN
            oldval := :old.v865;
            newval := :new.v865;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V866') AND 'V866' = r.column_name
         THEN
            oldval := :old.v866;
            newval := :new.v866;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V867') AND 'V867' = r.column_name
         THEN
            oldval := :old.v867;
            newval := :new.v867;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V868') AND 'V868' = r.column_name
         THEN
            oldval := :old.v868;
            newval := :new.v868;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V869') AND 'V869' = r.column_name
         THEN
            oldval := :old.v869;
            newval := :new.v869;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V870') AND 'V870' = r.column_name
         THEN
            oldval := :old.v870;
            newval := :new.v870;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V871') AND 'V871' = r.column_name
         THEN
            oldval := :old.v871;
            newval := :new.v871;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V872') AND 'V872' = r.column_name
         THEN
            oldval := :old.v872;
            newval := :new.v872;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V873') AND 'V873' = r.column_name
         THEN
            oldval := :old.v873;
            newval := :new.v873;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V874') AND 'V874' = r.column_name
         THEN
            oldval := :old.v874;
            newval := :new.v874;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V875') AND 'V875' = r.column_name
         THEN
            oldval := :old.v875;
            newval := :new.v875;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V876') AND 'V876' = r.column_name
         THEN
            oldval := :old.v876;
            newval := :new.v876;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V877') AND 'V877' = r.column_name
         THEN
            oldval := :old.v877;
            newval := :new.v877;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V878') AND 'V878' = r.column_name
         THEN
            oldval := :old.v878;
            newval := :new.v878;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V879') AND 'V879' = r.column_name
         THEN
            oldval := :old.v879;
            newval := :new.v879;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V880') AND 'V880' = r.column_name
         THEN
            oldval := :old.v880;
            newval := :new.v880;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V881') AND 'V881' = r.column_name
         THEN
            oldval := :old.v881;
            newval := :new.v881;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V882') AND 'V882' = r.column_name
         THEN
            oldval := :old.v882;
            newval := :new.v882;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V883') AND 'V883' = r.column_name
         THEN
            oldval := :old.v883;
            newval := :new.v883;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V884') AND 'V884' = r.column_name
         THEN
            oldval := :old.v884;
            newval := :new.v884;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V885') AND 'V885' = r.column_name
         THEN
            oldval := :old.v885;
            newval := :new.v885;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V886') AND 'V886' = r.column_name
         THEN
            oldval := :old.v886;
            newval := :new.v886;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V887') AND 'V887' = r.column_name
         THEN
            oldval := :old.v887;
            newval := :new.v887;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V888') AND 'V888' = r.column_name
         THEN
            oldval := :old.v888;
            newval := :new.v888;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V889') AND 'V889' = r.column_name
         THEN
            oldval := :old.v889;
            newval := :new.v889;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V890') AND 'V890' = r.column_name
         THEN
            oldval := :old.v890;
            newval := :new.v890;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V891') AND 'V891' = r.column_name
         THEN
            oldval := :old.v891;
            newval := :new.v891;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V892') AND 'V892' = r.column_name
         THEN
            oldval := :old.v892;
            newval := :new.v892;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V893') AND 'V893' = r.column_name
         THEN
            oldval := :old.v893;
            newval := :new.v893;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V894') AND 'V894' = r.column_name
         THEN
            oldval := :old.v894;
            newval := :new.v894;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V895') AND 'V895' = r.column_name
         THEN
            oldval := :old.v895;
            newval := :new.v895;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V896') AND 'V896' = r.column_name
         THEN
            oldval := :old.v896;
            newval := :new.v896;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V897') AND 'V897' = r.column_name
         THEN
            oldval := :old.v897;
            newval := :new.v897;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V898') AND 'V898' = r.column_name
         THEN
            oldval := :old.v898;
            newval := :new.v898;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V899') AND 'V899' = r.column_name
         THEN
            oldval := :old.v899;
            newval := :new.v899;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V900') AND 'V900' = r.column_name
         THEN
            oldval := :old.v900;
            newval := :new.v900;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V901') AND 'V901' = r.column_name
         THEN
            oldval := :old.v901;
            newval := :new.v901;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V902') AND 'V902' = r.column_name
         THEN
            oldval := :old.v902;
            newval := :new.v902;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V903') AND 'V903' = r.column_name
         THEN
            oldval := :old.v903;
            newval := :new.v903;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V904') AND 'V904' = r.column_name
         THEN
            oldval := :old.v904;
            newval := :new.v904;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V905') AND 'V905' = r.column_name
         THEN
            oldval := :old.v905;
            newval := :new.v905;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V906') AND 'V906' = r.column_name
         THEN
            oldval := :old.v906;
            newval := :new.v906;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V907') AND 'V907' = r.column_name
         THEN
            oldval := :old.v907;
            newval := :new.v907;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V908') AND 'V908' = r.column_name
         THEN
            oldval := :old.v908;
            newval := :new.v908;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V909') AND 'V909' = r.column_name
         THEN
            oldval := :old.v909;
            newval := :new.v909;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V910') AND 'V910' = r.column_name
         THEN
            oldval := :old.v910;
            newval := :new.v910;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V911') AND 'V911' = r.column_name
         THEN
            oldval := :old.v911;
            newval := :new.v911;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V912') AND 'V912' = r.column_name
         THEN
            oldval := :old.v912;
            newval := :new.v912;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V913') AND 'V913' = r.column_name
         THEN
            oldval := :old.v913;
            newval := :new.v913;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V914') AND 'V914' = r.column_name
         THEN
            oldval := :old.v914;
            newval := :new.v914;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V915') AND 'V915' = r.column_name
         THEN
            oldval := :old.v915;
            newval := :new.v915;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V916') AND 'V916' = r.column_name
         THEN
            oldval := :old.v916;
            newval := :new.v916;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V917') AND 'V917' = r.column_name
         THEN
            oldval := :old.v917;
            newval := :new.v917;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V918') AND 'V918' = r.column_name
         THEN
            oldval := :old.v918;
            newval := :new.v918;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V919') AND 'V919' = r.column_name
         THEN
            oldval := :old.v919;
            newval := :new.v919;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V920') AND 'V920' = r.column_name
         THEN
            oldval := :old.v920;
            newval := :new.v920;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V921') AND 'V921' = r.column_name
         THEN
            oldval := :old.v921;
            newval := :new.v921;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V922') AND 'V922' = r.column_name
         THEN
            oldval := :old.v922;
            newval := :new.v922;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V923') AND 'V923' = r.column_name
         THEN
            oldval := :old.v923;
            newval := :new.v923;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V924') AND 'V924' = r.column_name
         THEN
            oldval := :old.v924;
            newval := :new.v924;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V925') AND 'V925' = r.column_name
         THEN
            oldval := :old.v925;
            newval := :new.v925;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V926') AND 'V926' = r.column_name
         THEN
            oldval := :old.v926;
            newval := :new.v926;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V927') AND 'V927' = r.column_name
         THEN
            oldval := :old.v927;
            newval := :new.v927;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V928') AND 'V928' = r.column_name
         THEN
            oldval := :old.v928;
            newval := :new.v928;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V929') AND 'V929' = r.column_name
         THEN
            oldval := :old.v929;
            newval := :new.v929;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V930') AND 'V930' = r.column_name
         THEN
            oldval := :old.v930;
            newval := :new.v930;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V931') AND 'V931' = r.column_name
         THEN
            oldval := :old.v931;
            newval := :new.v931;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V932') AND 'V932' = r.column_name
         THEN
            oldval := :old.v932;
            newval := :new.v932;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V933') AND 'V933' = r.column_name
         THEN
            oldval := :old.v933;
            newval := :new.v933;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V934') AND 'V934' = r.column_name
         THEN
            oldval := :old.v934;
            newval := :new.v934;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V935') AND 'V935' = r.column_name
         THEN
            oldval := :old.v935;
            newval := :new.v935;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V936') AND 'V936' = r.column_name
         THEN
            oldval := :old.v936;
            newval := :new.v936;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V937') AND 'V937' = r.column_name
         THEN
            oldval := :old.v937;
            newval := :new.v937;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V938') AND 'V938' = r.column_name
         THEN
            oldval := :old.v938;
            newval := :new.v938;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V939') AND 'V939' = r.column_name
         THEN
            oldval := :old.v939;
            newval := :new.v939;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V940') AND 'V940' = r.column_name
         THEN
            oldval := :old.v940;
            newval := :new.v940;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V941') AND 'V941' = r.column_name
         THEN
            oldval := :old.v941;
            newval := :new.v941;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V942') AND 'V942' = r.column_name
         THEN
            oldval := :old.v942;
            newval := :new.v942;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V943') AND 'V943' = r.column_name
         THEN
            oldval := :old.v943;
            newval := :new.v943;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V944') AND 'V944' = r.column_name
         THEN
            oldval := :old.v944;
            newval := :new.v944;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V945') AND 'V945' = r.column_name
         THEN
            oldval := :old.v945;
            newval := :new.v945;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V946') AND 'V946' = r.column_name
         THEN
            oldval := :old.v946;
            newval := :new.v946;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V947') AND 'V947' = r.column_name
         THEN
            oldval := :old.v947;
            newval := :new.v947;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V948') AND 'V948' = r.column_name
         THEN
            oldval := :old.v948;
            newval := :new.v948;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V949') AND 'V949' = r.column_name
         THEN
            oldval := :old.v949;
            newval := :new.v949;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V950') AND 'V950' = r.column_name
         THEN
            oldval := :old.v950;
            newval := :new.v950;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V951') AND 'V951' = r.column_name
         THEN
            oldval := :old.v951;
            newval := :new.v951;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V952') AND 'V952' = r.column_name
         THEN
            oldval := :old.v952;
            newval := :new.v952;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V953') AND 'V953' = r.column_name
         THEN
            oldval := :old.v953;
            newval := :new.v953;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V954') AND 'V954' = r.column_name
         THEN
            oldval := :old.v954;
            newval := :new.v954;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V955') AND 'V955' = r.column_name
         THEN
            oldval := :old.v955;
            newval := :new.v955;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V956') AND 'V956' = r.column_name
         THEN
            oldval := :old.v956;
            newval := :new.v956;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V957') AND 'V957' = r.column_name
         THEN
            oldval := :old.v957;
            newval := :new.v957;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V958') AND 'V958' = r.column_name
         THEN
            oldval := :old.v958;
            newval := :new.v958;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V959') AND 'V959' = r.column_name
         THEN
            oldval := :old.v959;
            newval := :new.v959;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V960') AND 'V960' = r.column_name
         THEN
            oldval := :old.v960;
            newval := :new.v960;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V961') AND 'V961' = r.column_name
         THEN
            oldval := :old.v961;
            newval := :new.v961;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V962') AND 'V962' = r.column_name
         THEN
            oldval := :old.v962;
            newval := :new.v962;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V963') AND 'V963' = r.column_name
         THEN
            oldval := :old.v963;
            newval := :new.v963;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V964') AND 'V964' = r.column_name
         THEN
            oldval := :old.v964;
            newval := :new.v964;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V965') AND 'V965' = r.column_name
         THEN
            oldval := :old.v965;
            newval := :new.v965;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V966') AND 'V966' = r.column_name
         THEN
            oldval := :old.v966;
            newval := :new.v966;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V967') AND 'V967' = r.column_name
         THEN
            oldval := :old.v967;
            newval := :new.v967;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V968') AND 'V968' = r.column_name
         THEN
            oldval := :old.v968;
            newval := :new.v968;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V969') AND 'V969' = r.column_name
         THEN
            oldval := :old.v969;
            newval := :new.v969;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V970') AND 'V970' = r.column_name
         THEN
            oldval := :old.v970;
            newval := :new.v970;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V971') AND 'V971' = r.column_name
         THEN
            oldval := :old.v971;
            newval := :new.v971;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V972') AND 'V972' = r.column_name
         THEN
            oldval := :old.v972;
            newval := :new.v972;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V973') AND 'V973' = r.column_name
         THEN
            oldval := :old.v973;
            newval := :new.v973;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V974') AND 'V974' = r.column_name
         THEN
            oldval := :old.v974;
            newval := :new.v974;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V975') AND 'V975' = r.column_name
         THEN
            oldval := :old.v975;
            newval := :new.v975;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V976') AND 'V976' = r.column_name
         THEN
            oldval := :old.v976;
            newval := :new.v976;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V977') AND 'V977' = r.column_name
         THEN
            oldval := :old.v977;
            newval := :new.v977;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V978') AND 'V978' = r.column_name
         THEN
            oldval := :old.v978;
            newval := :new.v978;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V979') AND 'V979' = r.column_name
         THEN
            oldval := :old.v979;
            newval := :new.v979;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V980') AND 'V980' = r.column_name
         THEN
            oldval := :old.v980;
            newval := :new.v980;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V981') AND 'V981' = r.column_name
         THEN
            oldval := :old.v981;
            newval := :new.v981;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V982') AND 'V982' = r.column_name
         THEN
            oldval := :old.v982;
            newval := :new.v982;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V983') AND 'V983' = r.column_name
         THEN
            oldval := :old.v983;
            newval := :new.v983;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V984') AND 'V984' = r.column_name
         THEN
            oldval := :old.v984;
            newval := :new.v984;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V985') AND 'V985' = r.column_name
         THEN
            oldval := :old.v985;
            newval := :new.v985;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V986') AND 'V986' = r.column_name
         THEN
            oldval := :old.v986;
            newval := :new.v986;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V987') AND 'V987' = r.column_name
         THEN
            oldval := :old.v987;
            newval := :new.v987;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V988') AND 'V988' = r.column_name
         THEN
            oldval := :old.v988;
            newval := :new.v988;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V989') AND 'V989' = r.column_name
         THEN
            oldval := :old.v989;
            newval := :new.v989;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V990') AND 'V990' = r.column_name
         THEN
            oldval := :old.v990;
            newval := :new.v990;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V991') AND 'V991' = r.column_name
         THEN
            oldval := :old.v991;
            newval := :new.v991;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V992') AND 'V992' = r.column_name
         THEN
            oldval := :old.v992;
            newval := :new.v992;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V993') AND 'V993' = r.column_name
         THEN
            oldval := :old.v993;
            newval := :new.v993;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V994') AND 'V994' = r.column_name
         THEN
            oldval := :old.v994;
            newval := :new.v994;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V995') AND 'V995' = r.column_name
         THEN
            oldval := :old.v995;
            newval := :new.v995;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V996') AND 'V996' = r.column_name
         THEN
            oldval := :old.v996;
            newval := :new.v996;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V997') AND 'V997' = r.column_name
         THEN
            oldval := :old.v997;
            newval := :new.v997;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V998') AND 'V998' = r.column_name
         THEN
            oldval := :old.v998;
            newval := :new.v998;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V999') AND 'V999' = r.column_name
         THEN
            oldval := :old.v999;
            newval := :new.v999;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;

         IF UPDATING ('V1000') AND 'V1000' = r.column_name
         THEN
            oldval := :old.v1000;
            newval := :new.v1000;
            checkforfirsttime;
            colnames :=
                  colnames
               || 'colobj('''
               || r.column_name
               || ''','''
               || oldval
               || ''','''
               || newval
               || ''')';
         END IF;
      END LOOP;

      colnames := colnames || ')';
      strsql := strsql || colnames;
      strsql :=
         strsql
         || ', CURRENT_TIMESTAMP,TO_CHAR(SYSDATE, ''HH24:MI:SS''),
               USER,UTL_INADDR.GET_HOST_NAME, UTL_INADDR.GET_HOST_ADDRESS, 
               SYS_CONTEXT(''USERENV'',''TERMINAL''), 
               SYS_CONTEXT(''USERENV'', ''IP_ADDRESS'', 15))';

      EXECUTE IMMEDIATE strsql;
   END IF;
END;
/

DROP TABLE mytab CASCADE CONSTRAINTS;